apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: vault-debug-
  namespace: argo-workflows
spec:
  entrypoint: main
  serviceAccountName: argo-workflow-sa
  templates:
    - name: main
      container:
        image: hashicorp/vault:1.15
        command: [sh, -c]
        args:
          - |
            export VAULT_ADDR="http://vault.vault.svc.cluster.local:8200"
            export VAULT_TOKEN=$(vault write -field=token auth/kubernetes/login role=argo-workflow jwt=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token))
            
            echo "Attempting to get secret/platform/mctl-agent/database:"
            vault kv get secret/platform/mctl-agent/database
            
            echo "Listing secret/platform/mctl-agent:"
            vault kv list secret/platform/mctl-agent
