{%- if values.serviceType == "base-service" -%}
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.serviceName }}
  description: "${{ values.serviceName }} service deployed to preview"
  annotations:
    backstage.io/kubernetes-id: ${{ values.serviceName }}
    argocd/app-name: preview-${{ values.teamName }}-${{ values.serviceName }}
  labels:
    team: ${{ values.teamName }}
    env: preview
spec:
  type: service
  lifecycle: production
  owner: group:${{ values.teamName }}
{%- else -%}
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.serviceName }}
  description: "${{ values.serviceName }} worker deployed to preview"
  annotations:
    backstage.io/kubernetes-id: ${{ values.serviceName }}
    argocd/app-name: preview-${{ values.teamName }}-${{ values.serviceName }}
  labels:
    team: ${{ values.teamName }}
    env: preview
spec:
  type: worker
  lifecycle: production
  owner: group:${{ values.teamName }}
{%- endif %}
