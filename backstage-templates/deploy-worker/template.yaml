apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: deploy-worker
  title: Deploy Worker
  description: Build & deploy a background worker (deployment or cronjob) to the preview cluster
  tags:
    - deploy
    - kubernetes
    - preview
    - worker
spec:
  owner: group:admin
  type: service

  parameters:
    - title: Worker Info
      required:
        - teamName
        - serviceName
      properties:
        teamName:
          title: Team Name
          type: string
          description: "Select your team"
          ui:field: MyGroupsPicker
        serviceName:
          title: Worker Name
          type: string
          pattern: "^[a-z0-9][a-z0-9-]{0,62}$"

    - title: Source Repository
      required:
        - dockerfileRepo
      properties:
        dockerfileRepo:
          title: GitHub Repository
          type: string
          description: "owner/repo with Dockerfile (e.g. dmitriimashkov/my-worker)"
          pattern: "^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$"
        dockerfilePath:
          title: Dockerfile Path
          type: string
          default: Dockerfile
        repoPat:
          title: Repository PAT (private repos only)
          type: string
          description: "Fine-grained PAT with Contents:read scope for the source repo"
          ui:field: Secret

    - title: Secrets
      properties:
        secretEnvVars:
          title: Secret Environment Variables
          type: string
          description: "Plaintext KEY=value pairs (one per line). Example:\nBALANCE_ID=31808246\nBOT_TOKEN=abc123\nWISE_HOST=https://api.example.com"
          ui:widget: textarea
          ui:options:
            rows: 10
          ui:placeholder: |
            BALANCE_ID=31808246
            BOT_TOKEN=5504401510:AAEHW6AsTdsN6y30o-TOwSdFXOlgg3RscmM
            EXCHANGE_FEE_IN_PERCENT=1.75

  steps:
    - id: save-repo-pat
      name: Save repo PAT to Vault
      if: ${{ parameters.repoPat }}
      action: http:backstage:request
      input:
        method: POST
        path: /proxy/vault/v1/secret/data/preview/${{ parameters.teamName | parseEntityRef | pick('name') }}/${{ parameters.serviceName }}/repo-pat
        headers:
          Content-Type: application/json
        body:
          data:
            pat: ${{ parameters.repoPat }}

    - id: trigger-build
      name: Build & deploy via GitHub Actions
      action: github:actions:dispatch
      input:
        repoUrl: github.com?repo=mctl.me&owner=dmitriimashkov
        workflowId: service.yml
        branchOrTagName: main
        workflowInputs:
          action: onboard
          team_name: ${{ parameters.teamName | parseEntityRef | pick('name') }}
          service_name: ${{ parameters.serviceName }}
          service_type: worker-service
          dockerfile_repo: ${{ parameters.dockerfileRepo }}
          dockerfile_path: ${{ parameters.dockerfilePath | default('Dockerfile') }}
          secret_env_vars: ${{ parameters.secretEnvVars | default('') }}

  output:
    links:
      - title: GitHub Actions Run
        url: https://github.com/dmitriimashkov/mctl.me/actions/workflows/service.yml
