{
  "$comment": "Source of truth for the Cloudflare MCP portal mapping of server `api` on portal `mcp` (mctlhq/.github#35, mctlhq/mctl-api#276). Every tool this server registers must appear here with an explicit decision; internal/mcp/portal_allowlist_test.go fails the build otherwise. An enabled tool must carry a `reason` saying what it exposes or changes and why that is acceptable on a shared surface; a tool that is NOT recorded readOnly in internal/mcp/annotations_test.go may be enabled only if the mutatingOnPortal map in that test names it, which is a reviewed Go change rather than a JSON edit. readOnlyHint is a side-effect hint, not a privacy classification. Apply with scripts/portal-allowlist-apply.sh.",
  "portal": "mcp",
  "server": "api",
  "default_disabled": true,
  "tools": [
    {
      "name": "mctl_acknowledge_incident",
      "enabled": true,
      "reason": "Marks an incident acknowledged, changing platform incident state other operators read. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_add_custom_domain",
      "enabled": true,
      "reason": "Registers a tenant custom domain and mints a DNS ownership challenge; changes routing intent. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_apply_openclaw_resource_profile",
      "enabled": true,
      "reason": "Rewrites an OpenClaw agent's CPU/memory profile in gitops; changes what the tenant's pod gets scheduled with. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_approve_dev_loop",
      "enabled": true,
      "reason": "Signals approval into a running DevLoopWorkflow, releasing an agent to act on a repository. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_bind_agent_release",
      "enabled": false
    },
    {
      "name": "mctl_create_agent",
      "enabled": true,
      "reason": "Creates an agent record, adding a new actor that can later be published and run. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_create_preview",
      "enabled": true,
      "reason": "Provisions a preview environment: creates namespaced workloads and consumes cluster quota. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_create_tenant",
      "enabled": true,
      "reason": "Creates a tenant: namespace, quota and gitops scaffolding. A structural change to the platform. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_delete_openclaw_identity",
      "enabled": true,
      "reason": "Deletes a tenant identity override file from gitops; the agent falls back to image defaults. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_delete_openclaw_skill",
      "enabled": true,
      "reason": "Deletes a tenant skill from gitops, removing a capability from the running agent. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_delete_preview",
      "enabled": true,
      "reason": "Destroys a preview environment and its workloads. Irreversible for that preview. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_delete_tenant",
      "enabled": true,
      "reason": "Deletes a tenant with its namespace, database and gitops entries. The most destructive call on this surface. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_deploy_openclaw",
      "enabled": true,
      "reason": "Deploys an OpenClaw agent for a tenant, changing what runs for that tenant. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_deploy_service",
      "enabled": true,
      "reason": "Deploys a service image tag: changes what runs in production for the named team and service. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_deprecate_platform_skill",
      "enabled": true,
      "reason": "Marks a platform skill deprecated, changing what tenants are offered going forward. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_disable_tenant_skill",
      "enabled": true,
      "reason": "Unbinds a platform skill from a tenant, removing that capability from its agent. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_enable_tenant_skill",
      "enabled": true,
      "reason": "Binds a platform skill to a tenant, granting its agent a new capability. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_get_agent",
      "enabled": false
    },
    {
      "name": "mctl_get_epic_status",
      "enabled": false,
      "reason": "Read-only roadmap epic status from the published RoadmapPublication (public mctlhq/.github data, no credentials). Not yet enabled on the shared portal: that is an owner decision, not part of mctl-api#333."
    },
    {
      "name": "mctl_get_ready_work_items",
      "enabled": false,
      "reason": "Read-only list of ready roadmap work items from the published RoadmapPublication (public mctlhq/.github data, no credentials). Not yet enabled on the shared portal: that is an owner decision, not part of mctl-api#333."
    },
    {
      "name": "mctl_plan_epic_wave",
      "enabled": false,
      "reason": "Read-only dry run of a roadmap epic wave from the published RoadmapPublication (mctl-api#334); starts nothing. Not enabled on the shared portal: that is an owner decision."
    },
    {
      "name": "mctl_start_epic_wave",
      "enabled": false,
      "reason": "Starts one DevLoopWorkflow per item of a publication-bound wave plan (mctl-api#334). Admin-only and paid per started DevLoop; not for the shared portal."
    },
    {
      "name": "mctl_get_dev_loop",
      "enabled": true,
      "reason": "Liveness of one DevLoop workflow by id: status and phase only, no code, no secrets; the upstream refuses ids outside the caller's tenants."
    },
    {
      "name": "mctl_get_incident",
      "enabled": true,
      "reason": "Incident record with collected evidence (log excerpts, analysis, PR links); tenant-scoped by the upstream. Excerpts can carry application log text, acceptable on a private admin-only aggregate."
    },
    {
      "name": "mctl_get_lifecycle_ownership",
      "enabled": true,
      "reason": "Read-only view of the lifecycle ownership store: who is responsible for an entity phase, whether that owner is alive, and whether the store agrees with the old DevLoopWorkflow check. It exposes internal repo/PR identifiers and owner ids, no credentials, and grants no authority -- ownership says who is responsible, never what they may do. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication and admin check are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_inspect_lifecycle_conflict",
      "enabled": true,
      "reason": "Read-only evidence for a lifecycle recovery decision: the stored row, derived status, the legacy DevLoopWorkflow answer, divergence class, recent transitions, and the precondition values a recovery call must send. Same exposure class as mctl_get_lifecycle_ownership -- internal repo/PR identifiers and owner ids, no credentials, no authority. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication and admin check are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_request_lifecycle_reconcile",
      "enabled": false,
      "reason": "Human-operator lifecycle recovery transition, not yet vetted for the shared portal: it appends an audited event but the recovery surface as a whole is new and disabled here pending operational experience."
    },
    {
      "name": "mctl_fence_lifecycle_claim",
      "enabled": false,
      "reason": "Human-operator lifecycle recovery transition that releases a claim; disabled on the shared portal pending operational experience with the new recovery surface, matching the other three mutating recovery tools."
    },
    {
      "name": "mctl_request_lifecycle_handoff",
      "enabled": false,
      "reason": "Human-operator lifecycle recovery transition that starts a handoff off a stuck owner; disabled on the shared portal pending operational experience with the new recovery surface."
    },
    {
      "name": "mctl_retry_lifecycle_handoff",
      "enabled": false,
      "reason": "Human-operator lifecycle recovery transition that re-arms a stalled handoff; disabled on the shared portal pending operational experience with the new recovery surface."
    },
    {
      "name": "mctl_get_openclaw_sizing_recommendation",
      "enabled": true,
      "reason": "Aggregated VictoriaMetrics resource history and a recommended profile for one OpenClaw service; numbers only, tenant-scoped upstream."
    },
    {
      "name": "mctl_get_operation",
      "enabled": true,
      "reason": "Schema of one platform operation: parameter names, types, defaults and risk level. Catalogue metadata with no tenant data."
    },
    {
      "name": "mctl_get_resource_usage",
      "enabled": true,
      "reason": "CPU, memory and pod quota usage for a team workspace; capacity figures only, tenant-scoped by the upstream."
    },
    {
      "name": "mctl_get_service_config",
      "enabled": true,
      "reason": "Service configuration as recorded in GitOps: image tag, host, port, component type, database flag. No secret values; tenant-scoped upstream."
    },
    {
      "name": "mctl_get_service_logs",
      "enabled": true,
      "reason": "Raw recent log lines from Loki for a service the caller may read. Logs can contain application data; the upstream enforces tenant scope and the portal is private to admins and pilot members, so the exposure equals what the caller already has."
    },
    {
      "name": "mctl_get_service_status",
      "enabled": true,
      "reason": "ArgoCD sync and health state plus the service's recorded configuration; operational metadata, tenant-scoped upstream."
    },
    {
      "name": "mctl_get_tenant",
      "enabled": true,
      "reason": "Team workspace details: members, quotas, deployed services. Member logins are platform identities the caller can already see; tenant-scoped upstream."
    },
    {
      "name": "mctl_get_workflow_logs",
      "enabled": true,
      "reason": "Argo Workflow step logs from the archive for a workflow the caller may read. Same class as service logs: may carry build output; tenant-scoped upstream, private portal."
    },
    {
      "name": "mctl_get_workflow_status",
      "enabled": true,
      "reason": "Status and step summary of one Argo Workflow run; operational metadata, tenant-scoped upstream."
    },
    {
      "name": "mctl_grant_repo_access",
      "enabled": true,
      "reason": "Grants a tenant access to a repository: an authorization change, not a deployment one. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_incident_summary",
      "enabled": true,
      "reason": "Aggregate counts of active incidents by status, severity and type; no incident bodies, no tenant data."
    },
    {
      "name": "mctl_list_agent_executions",
      "enabled": true,
      "reason": "Durable execution records of DevLoop runs: agent version, workflow id, result. Pipeline metadata; tenant-scoped upstream."
    },
    {
      "name": "mctl_list_agent_versions",
      "enabled": true,
      "reason": "Published versions of an mctl-agents agent with manifest, git SHA, image reference and prompt hash; release metadata, no tenant data."
    },
    {
      "name": "mctl_list_agents",
      "enabled": false
    },
    {
      "name": "mctl_list_domains",
      "enabled": true,
      "reason": "Custom and generated domains for a team or service with verification status; hostnames only, tenant-scoped upstream."
    },
    {
      "name": "mctl_list_incidents",
      "enabled": true,
      "reason": "Incident list with status, severity and one-line summary; summaries are alert titles, tenant-filtered by the upstream."
    },
    {
      "name": "mctl_list_openclaw_identity",
      "enabled": true,
      "reason": "File names of a team's OpenClaw identity overrides in GitOps; names only, tenant-scoped upstream."
    },
    {
      "name": "mctl_list_openclaw_skills",
      "enabled": true,
      "reason": "File names of a team's OpenClaw skills in GitOps; names only, tenant-scoped upstream."
    },
    {
      "name": "mctl_list_operations",
      "enabled": true,
      "reason": "Catalogue of platform operations with parameters and risk levels; discovery metadata, no tenant data."
    },
    {
      "name": "mctl_list_platform_skills",
      "enabled": true,
      "reason": "Platform-wide skills visible to the caller after visibility and binding filters; catalogue metadata the upstream already scopes."
    },
    {
      "name": "mctl_list_previews",
      "enabled": true,
      "reason": "Preview environments of a team with health and sync state; namespace names and status, tenant-scoped upstream."
    },
    {
      "name": "mctl_list_recent_agent_runs",
      "enabled": true,
      "reason": "Last ten mctl-agents triggers: operation, mode, service, status, who triggered; operator activity metadata, admin-scoped by the upstream."
    },
    {
      "name": "mctl_list_recent_operations",
      "enabled": true,
      "reason": "Recent audit-log entries: who ran which operation when and the workflow it started. Operator activity the caller may already see; upstream-scoped."
    },
    {
      "name": "mctl_list_repos",
      "enabled": true,
      "reason": "GitHub repositories the team's App installations expose for deployment; repository names and visibility, tenant-scoped upstream."
    },
    {
      "name": "mctl_list_services",
      "enabled": true,
      "reason": "Deployed services with team, image tag, host and database flag; operational metadata, tenant-filtered by the upstream."
    },
    {
      "name": "mctl_list_tenant_skill_bindings",
      "enabled": true,
      "reason": "Skill bindings per tenant; admins see all, members their own \u2014 the upstream applies that filter, the tool exposes only binding names."
    },
    {
      "name": "mctl_list_tenants",
      "enabled": true,
      "reason": "All team workspaces with quotas and member counts; admin-only at the upstream, which refuses non-admins (measured through the portal)."
    },
    {
      "name": "mctl_list_workflows",
      "enabled": true,
      "reason": "Recent Argo Workflow runs for a team's namespace: names, status, timestamps; tenant-scoped upstream."
    },
    {
      "name": "mctl_promote_agent",
      "enabled": true,
      "reason": "Promotes an agent version to a stage, changing which version tenants resolve to. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_provision_database",
      "enabled": true,
      "reason": "Provisions a database for a service and wires its credentials; creates persistent state. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_publish_agent_definition_version",
      "enabled": false
    },
    {
      "name": "mctl_publish_agent_profile_version",
      "enabled": false
    },
    {
      "name": "mctl_publish_agent_version",
      "enabled": true,
      "reason": "Publishes an agent version, making it resolvable and runnable by tenants. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_publish_platform_skill",
      "enabled": true,
      "reason": "Publishes a platform skill, offering it to every tenant that binds it. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_read_openclaw_identity",
      "enabled": true,
      "reason": "Raw content of one identity override file (AGENTS.md, SOUL.md, IDENTITY.md, USER.md, TOOLS.md) from the team's GitOps directory; prompt text the team authored, tenant-scoped upstream."
    },
    {
      "name": "mctl_read_openclaw_skill",
      "enabled": true,
      "reason": "Raw content of one OpenClaw skill file from the team's GitOps directory; team-authored text, tenant-scoped upstream."
    },
    {
      "name": "mctl_read_platform_skill",
      "enabled": true,
      "reason": "Content of a platform-wide skill from the GitOps catalogue, subject to the caller's visibility; catalogue text, no tenant data."
    },
    {
      "name": "mctl_remove_custom_domain",
      "enabled": true,
      "reason": "Removes a tenant custom domain, withdrawing routing for that hostname. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_resolve_agent",
      "enabled": true,
      "reason": "Which agent version is released to an environment; release pointer only, no tenant data."
    },
    {
      "name": "mctl_resolve_incident",
      "enabled": true,
      "reason": "Closes an incident, changing the state other operators and alerts read. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_resume_openclaw_deploy",
      "enabled": true,
      "reason": "Resumes a paused OpenClaw deployment, letting a held rollout proceed. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_retire_service",
      "enabled": true,
      "reason": "Retires a service: removes its workloads and gitops entry. Recovery means re-deploying it. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_rollback_agent",
      "enabled": true,
      "reason": "Rolls an agent back to a prior version, changing what tenants run. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_rollback_agent_binding",
      "enabled": false
    },
    {
      "name": "mctl_rollback_service",
      "enabled": true,
      "reason": "Rolls a service back to a prior image tag, changing production. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_save_openclaw_identity",
      "enabled": true,
      "reason": "Writes a tenant identity override into gitops; the live agent picks it up within minutes. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_save_openclaw_skill",
      "enabled": true,
      "reason": "Writes a tenant skill into gitops, adding a capability to the running agent. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_scale_service",
      "enabled": true,
      "reason": "Changes a service's replica count, consuming or releasing cluster capacity. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_set_agent_version_lifecycle",
      "enabled": false
    },
    {
      "name": "mctl_sync_repos",
      "enabled": true,
      "reason": "Re-syncs the repository inventory, rewriting what the platform believes it owns. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_agents_run",
      "enabled": true,
      "reason": "Starts an agents run: model spend and repository writes follow from it. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_approve",
      "enabled": true,
      "reason": "Flips a proposal to accepted in gitops, which is what lets the implementer act on it. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_implementer",
      "enabled": true,
      "reason": "Starts an implementer run that writes code and opens a pull request. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_incident_responder",
      "enabled": true,
      "reason": "Starts the incident responder, which acts on live platform state. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_issue",
      "enabled": true,
      "reason": "Starts the DevLoop for an issue: investigation, proposal and, after approval, code. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_mentor_only",
      "enabled": true,
      "reason": "Starts a mentor-only agent pass, which comments on work in the repository. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_portal_server_auth_apply",
      "enabled": true,
      "reason": "Starts the mctl-gitops workflow that applies the committed OAuth registration -- above all the scope -- of the MCP portal's own upstream servers; it holds no Cloudflare credential and the write still needs a required reviewer on a protected environment, so a caller here can ask for that apply but cannot perform it. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_reconcile",
      "enabled": true,
      "reason": "Starts a reconcile pass that may move workflow and proposal state. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_shepherd",
      "enabled": true,
      "reason": "Starts the PR shepherd, which pushes fixes and can merge pull requests. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_trigger_single_service",
      "enabled": true,
      "reason": "Starts an agents run scoped to one service, with the same spend and write effects. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_verify_domain",
      "enabled": true,
      "reason": "Verifies a domain challenge and, on success, triggers the ingress and certificate workflow. Enabled on the shared portal by owner decision 2026-09-12 (mctlhq/.github#35): the portal switch is user-blind, so the mctl API's own authentication, team scope and role checks are the access control -- the portal only decides visibility."
    },
    {
      "name": "mctl_whoami",
      "enabled": true,
      "reason": "Returns only the caller's own user id, admin flag, teams and namespaces \u2014 the identity probe the aggregate portal's A\u2260B check calls; nothing about anyone else."
    }
  ]
}
