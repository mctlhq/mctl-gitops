# Inbox 2026-08-08

## Source: github releases
- **Backstage v1.53.1 (patch, ~end of July 2026)** — Fixes TypeScript configuration schema errors so they no longer prevent apps from building/starting; now logged as warnings instead. https://github.com/backstage/backstage/releases
- **Backstage v1.54.0-next.0/next.1/next.2 pre-releases** — Chain of pre-releases ahead of the next stable cut; no stable v1.54.0 tag yet as of this run. https://github.com/backstage/backstage/releases
- **Backstage releases page date anomaly** — WebFetch returned 2024 dates for the v1.54.0-next.x and v1.53.1 entries, which is inconsistent with our known timeline (v1.53.0 shipped July 14, 2026); version numbers/ordering are likely correct but dates need re-verification via another source (same caveat as the 2026-07-18 run had for TypeScript/Playwright).
- **No stable Backstage v1.53.0 successor confirmed yet** — v1.53.0 (July 14, 2026) with the community-plugins compat wait window (per ADR/architecture.md) is still our most recent stable target; nothing new above it apart from the v1.53.1 patch above.
- **Node.js July 2026 security release — v22.23.2 / v24.18.1 / v26.5.1 (July 29, 2026)** — New security batch, 11 CVEs (3 High, 5 Medium, 3 Low) across all active lines, superseding the June 18 batch reported on 2026-07-18. https://nodejs.org/en/blog/vulnerability/july-2026-security-releases
- **Node.js v26.6.0 (Aug 3) and v26.7.0 (Aug 5, 2026)** — Current-line feature releases after the security patch; v26.7.0 adds perfetto tracing support, crypto and test-runner improvements. Not directly relevant (we pin 22||24) but confirms the line is active.
- **Node.js v24.19.0 (Aug 3, 2026)** — LTS "Krypton" feature release after the July 29 security patch (v24.18.1); adds blob text streaming and TLS group reporting.
- **Playwright v1.62.1 (July 30, 2026)** — Patch release fixing 5 regressions from v1.62 (TypeScript config "extends" resolution, tsconfig project references, a11y snapshot button-name drop, page.evaluate() type-check for branded primitives, image elements in a11y snapshots). This is newer than the v1.61.x found on 2026-07-18 (whose dates were themselves flagged unreliable); v1.62.1 exists as a distinct, higher version regardless.
- **Prettier 3.9.6 (July 21, 2026)** — Adds `import defer` support for TypeScript, preserves quotes for methods named `new`, and introduces a new official plugin `@prettier/plugin-yuku`. Newer than the 3.9.5 baseline noted 2026-07-18.
- **TypeScript releases page still returning stale/inconsistent data** — WebFetch again showed 6.0.3 (April 2025) as "latest" with 5.9.3 (Oct 2024) listed below it — same anomaly flagged on 2026-07-18, unresolved; direct npm registry lookup (npmjs.com/package/typescript) returned HTTP 403 this run, so no independent confirmation was possible. Needs a different lookup method (e.g. `npm view typescript version` via CLI) next cycle.
- **Yarn — no new v1.x releases checked this run** — not re-verified; last known baseline v1.22.22 unchanged as of 2026-07-18.

## Source: cve
- **Node.js July 2026 security batch — 11 CVEs (3 High, 5 Medium, 3 Low), shipped as v22.23.2/v24.18.1/v26.5.1 (July 29, 2026)** — supersedes the June 18 batch. Highlights: CVE-2026-56848 (HTTP/2 heap use-after-free in nghttp2_session_mem_send() when called re-entrantly during nghttp2_session_mem_recv(), High severity, potential RCE per multiple writeups), a Permission Model bypass allowing sandboxed code to escape the allowlist, a node:sqlite stale-iterator bug (StatementSyncIterator continuing to execute after reset/rebind via DatabaseSync#createTagStore()), and a dns.resolveAny() abort when a DNS response contains more than 256 A records. Directly affects our engines target (22||24). https://nodejs.org/en/blog/vulnerability/july-2026-security-releases
- **GHSA-7hv8-3fr9-j2hv correction** — follow-up confirms this is the long-patched **CVE-2023-25571** (Backstage Software Catalog XSS via javascript: URLs in catalog entities, fixed in @backstage/catalog-model 1.1.5 / core-components 0.12.3 / plugin-catalog-backend 1.7.1, published Feb 2023) — not a new 2026 advisory. The 2026-07-18 inbox left this unresolved/pending follow-up; it is stale and long since patched upstream, well below any version we'd run. https://github.com/backstage/backstage/security/advisories/GHSA-7hv8-3fr9-j2hv
- **CVE-2026-29185 — Backstage SCM URL parsing vulnerability** — found via search (SentinelOne vuln DB reference), affects Backstage prior to version 1.20.1; far below our current baseline, listed here only because it surfaced in an "August 2026 Backstage CVE" search, not because it's new or applicable. https://www.strix.ai/cve/CVE-2026-29185
- **No new Backstage CVEs found for the ~July 18 – Aug 8 window beyond what was already reported on 2026-07-18** (CVE-2026-24048 SSRF, CVE-2026-24046 symlink traversal, CVE-2026-32236 auth-backend SSRF, CVE-2026-32237 scaffolder dry-run secret leak) — searches this run surfaced only the same set plus older/unrelated CVEs (2023, pre-1.20.1).
- **No new React/Next.js or Material UI CVEs found since 2026-07-18** — search repeated the same React Server Components DoS cluster (CVE-2026-23864/23869/23870, already tracked) with no new entries; still no Material UI-specific advisory located for 2026.
- **No Backstage community-plugins-specific CVE found** — searches return only the general community-plugins repo description (kubernetes/techdocs workspaces) and older advisories (a TechDocs mkdocs.yml RCE patched in 1.14.3, a scaffolder template-write RCE), neither dated to the 2026-07-18–2026-08-08 window; not confirmed as new.

## Source: mctl metrics
- **mctl-portal service status (tenant admins)** — ArgoCD app `admins-mctl-portal` health=Healthy, syncStatus=Synced, revision `2.6.3` (unchanged from 2026-07-18), but `updatedAt` moved to 2026-08-07T23:59:44Z (was 2026-07-17T23:59:46Z) — looks like a routine daily resync rather than a version bump, since the revision string is identical.
- **`mctl_get_service_status` again returns `"service": null`** — same gap noted on 2026-07-18, still unresolved this run.
- **`mctl_get_service_config` still fails** — same error "service not found: admins/mctl-portal" reproduced again today, consistent with the 2026-07-18 finding; the admins/mctl-portal config lookup gap persists across at least 3 weeks.
- **Resource usage — tenant admins** — allocated: limits.cpu=10, limits.memory=5Gi, pods=12; used: limits.cpu=8500m (85%, up from 75.5% on 2026-07-18), limits.memory=4224Mi (~82.5%, up from 72.5%), pods=8/12, requests.cpu=940m (47%), requests.memory=1968Mi (~64%). Both CPU and memory limit-usage percentages have risen noticeably over the past 3 weeks.
- **Resource usage — tenant labs** — allocated: limits.cpu=12, limits.memory=10.5Gi, pods=25 (was 15 on 2026-07-18 — quota increased); used: limits.cpu=8700m (72.5%), limits.memory=5280Mi (~50%), pods=11/25, requests.cpu=1035m (34.5%), requests.memory=2576Mi (~51.5%). Per standing CLAUDE.md instruction, `labs` is flagged as close to its memory limit, but current numbers (~50-51.5% of allocated memory) again do not show an acute limit-memory situation; note the pod quota for `labs` grew from 15 to 25 pods since the last check.

## Source: mctl incidents
- **No active incidents for mctl-portal** — `mctl_list_incidents(team=admins, service=mctl-portal)` returned `count: 0`, empty items list, same as 2026-07-18.
