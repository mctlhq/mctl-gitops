# Tasks: issue-466-fix-server-retry-the-initial-database-co

- [ ] 1. Add `OpenWithRetry(ctx context.Context, dsn string, maxOpenConns, maxIdleConns int, interval, timeout time.Duration) (*sql.DB, error)` to `internal/db` (new file `internal/db/retry.go`, or appended to `db.go`), implemented as a loop calling the existing `Open` (through an injectable seam, see below) on each attempt, returning a single non-retrying `Open` result when `driverFor(dsn)` reports a non-Postgres driver, logging one line per attempt via `slog`, waiting `interval` between attempts via `select` on `time.After(interval)` and `ctx.Done()`, and stopping once cumulative elapsed time exceeds `timeout` (e.g. via `context.WithTimeout(ctx, timeout)` wrapping the loop, or a manual `time.Now()` deadline check) — DoD: `Open` itself is untouched (same signature, same single-attempt behavior); a `file:` DSN never waits (one attempt, error returned as-is); the per-attempt call and the logger are passed as parameters into an unexported `openWithRetry` (no package-level mock variable, no `slog.Default()` swap); reaching the deadline returns the LAST observed database error wrapped with the bound, never a bare `context.DeadlineExceeded`; `OpenWithRetry` returns `(*sql.DB, nil)` immediately on the first successful attempt with an `attempts=0` info log, retries on any error from `Open`, returns a wrapped error naming the timeout after the deadline, and returns `ctx.Err()` promptly if `ctx` is canceled mid-wait; `go vet ./...` and `go build ./...` pass.
- [ ] 2. Wire `cmd/server/main.go` to call `db.OpenWithRetry` instead of `db.Open` at the current call site (`cmd/server/main.go:83-86`), passing a short fixed interval and bounded timeout (e.g. 2s / 2m as package-level constants near `version` at the top of `main.go`, named e.g. `dbConnectRetryInterval` / `dbConnectRetryTimeout`), keeping the existing `slog.Error("db open", "err", err); os.Exit(1)` on final failure unchanged (depends on 1) — DoD: a successful first attempt behaves identically to today (same `rawDB`, same subsequent `db.Migrate` call and `defer rawDB.Close()`); a database that never becomes reachable within the timeout still logs `ERROR db open` and exits 1, same as before this change from the operator's point of view (just after the bounded wait).
- [ ] 3. Update package-level doc comment on `Open` in `internal/db/db.go` to note that it performs exactly one attempt and that `OpenWithRetry` is the bounded-retry variant used by the server's startup path, so future readers/callers pick the right one (depends on 1) — DoD: comment reviewed for accuracy against the final implementation; no behavior change.
- [ ] 4. Run `go fmt ./...`, `go vet ./...`, and the full test suite (`go test ./...`) locally against the changed packages (`internal/db`, `cmd/server`) and fix any fallout (depends on 1, 2) — DoD: all three commands exit clean; no pre-existing test broken by the new retry wrapper's presence (the unmodified `Open` keeps `internal/db/db_test.go`'s `TestOpenPool` passing unchanged).

## Tests

- [ ] T1. Loop-mechanics test in `internal/db` (e.g. `internal/db/retry_test.go`) driving the injected attempt seam rather than a real database, so it is deterministic and does not sleep out a real deadline. Assert all four: (a) an attempt that fails twice then succeeds returns a non-nil `*sql.DB` and no error, and the seam was called exactly three times; (b) an attempt that always fails returns an error naming the timeout and does not run past it; (c) a first-attempt success calls the seam exactly once and never waits; (d) a `file:` DSN calls the seam exactly once even when it fails, proving the SQLite branch skips the loop; (e) when the deadline is reached, the returned error wraps the last error the attempt produced and does NOT collapse to `context.DeadlineExceeded` — assert on a distinctive sentinel returned by the fake, since this is the diagnostic the issue exists to produce. **Validate by mutation before finalizing**: replace the loop body with a single call and confirm (a) fails; restore, then remove the SQLite branch and confirm (d) fails. A test that only exercises the success path passes on today's code and proves nothing.
- [ ] T2. Unit test asserting `OpenWithRetry` returns promptly (well under `timeout`) when its `ctx` is canceled mid-wait (e.g. cancel after the first failed attempt) — guards the shutdown-promptness requirement in requirements.md.
- [ ] T3. Unit test asserting the log output by passing a `*slog.Logger` writing into a `bytes.Buffer` the test owns (the `captureLogs` helper at `internal/workertoken/tokenhandler_test.go:364` shows the JSON-handler-over-buffer shape, but do NOT copy its `slog.SetDefault` swap here — the logger is a parameter in this design precisely so the test needs no global mutation) shows the attempt count distinguishing a zero-retry success from an N-retry success, per the issue's "reachable after 0 retries" vs "reachable after 7 retries" acceptance criterion.
- [ ] T4. Confirm existing `internal/db/db_test.go` `TestOpenPool` still passes unmodified, proving `Open`'s single-attempt contract was not disturbed.
- [ ] T5. Manual/staging validation (outside CI, tracked as a follow-up rather than blocking merge): after deploy, confirm on `mctl-preprod` that a rollout coinciding with the netpol race no longer produces an `ERROR db open` log line or an extra container restart, per the issue's stated acceptance ("A rollout with no `ERROR db open` line and no container restart").

## Rollback

Revert the commit(s) implementing `OpenWithRetry` and its call site change in
`cmd/server/main.go` — `Open`'s untouched signature means `git revert` (or a tag
rollback via `mctl_rollback_service`) restores today's immediate-exit-on-first-failure
behavior with no data or schema implications, since this change touches only
in-process startup control flow and adds no persistent state, migration, or new
external dependency. No feature flag is needed: the change is a pure code revert.
