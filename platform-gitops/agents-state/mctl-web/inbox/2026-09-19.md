# Inbox 2026-09-19

## Source: github releases
- **Nuxt latest release v4.5.2** — released August 5; pinned version in context/architecture.md is 4.3.1. https://github.com/nuxt/nuxt/releases/latest
- **Vue core latest release v3.5.43** — released September 17; pinned version is 3.5.30. https://github.com/vuejs/core/releases/latest
- **vue-router latest release fetched as v5.3.1** — page reported release date September 2, 2024; pinned version is 4.6.4 (fetch result looks stale/inconsistent with a 2024 date on a 2026 "latest" query — flagging as-is). https://github.com/vuejs/router/releases/latest
- **vee-validate latest release v4.15.1** — released June 7; matches the version pinned in context/architecture.md (4.15.1). https://github.com/logaretm/vee-validate/releases/latest
- **yup latest release fetched as v1.0.0** — page reported release date Feb 8; pinned version in architecture.md is 1.7.1, which is higher than what the fetch returned (fetch result looks stale/incomplete — flagging as-is). https://github.com/jquense/yup/releases/latest
- **@vueuse/core latest release v15.0.0** — released September 16; pinned version is 14.2.1. Release notes mention breaking changes: removed templateRef support, dropped Node.js 20 support, removed deprecated timer options in favor of a `scheduler` param; new `useWebMCP`, `useLiveAnnouncer`, `useTemporalNow`. https://github.com/vueuse/vueuse/releases/latest
- **dart-sass latest release 1.104.1** — released September 12; pinned version is 1.98.0. Includes a comment-duplication fix before `@use` rules and a parser-crash fix for invalid selectors in indented syntax. https://github.com/sass/dart-sass/releases/latest
- **cloudflare/workers-sdk latest release @cloudflare/containers-shared@0.17.0** — released September 18; adds container building support for experimental Build Output in Wrangler/Vite plugin, reorganizes container output paths, bumps several @cloudflare/* sub-package deps. https://github.com/cloudflare/workers-sdk/releases/latest
- **cloudflare/workerd latest release v1.20260918.1** — released September 18; updates V8 to 15.4, wires in a new Rust I/O backend (`--//:io_backend`, on by default), adds an inbound UDP handler. https://github.com/cloudflare/workerd/releases/latest

## Source: cve
- **CVE-2026-71316 (Nuxt, CVSS 7.5, HIGH)** — affects Nuxt 4.4.0 until 4.5.1: `cache:nuxt:payload` entries for `/<page>/_payload.json` can be returned before route middleware/page guards run, disclosing another user's SSR data (published Aug 5, last modified Sep 8). https://www.strix.ai/cve/CVE-2026-71316
- **CVE-2026-71320 (Nuxt, CVSS 8.1, HIGH)** — affects Nuxt 3.4.0–3.21.10 and up to 4.5.1: template-key injection via `/__nuxt_island/` props when `vue.runtimeCompiler: true`, leading to template execution in the Nitro process; fixed in 3.21.10 / 4.5.1. https://www.strix.ai/cve/CVE-2026-71320
- **CVE-2026-71318 (Nuxt, CVSS 4.8, MEDIUM)** — affects Nuxt 3.1.0–3.21.10 and up to 4.5.1: attacker-supplied top-level `as` prop on `/__nuxt_island/` endpoint drives dynamic component resolution (`<component :is>`, `resolveDynamicComponent`, `h()`); fixed in 3.21.10 / 4.5.1. https://www.strix.ai/cve/CVE-2026-71318
- **CVE-2026-61793 (nuxt-og-image module, unauthenticated SSRF)** — SSRF via `fonts[].path` URL parameter in the nuxt-og-image module; not confirmed whether mctl-web uses this module, noting for completeness. https://advisories.gitlab.com/npm/nuxt-og-image/CVE-2026-61793/
- **CVE-2026-39364 (Vite, CVSS 8.2, HIGH, actively exploited)** — dev-server file-access bypass: query params like `?raw`, `?import&raw`, `?import&url&inline` can retrieve files blocked by `server.fs.deny` (e.g. `.env`, `*.crt`) with HTTP 200; affects Vite 7.1.0–7.3.1 and 8.0.0–8.0.4, fixed in 7.3.2 / 8.0.5+. Under active mass-scanning exploitation since August 2026 (F5 Labs: ~807 grouped attacks, ~32,000 events in one month) targeting exposed dev servers for cloud credential theft. https://thehackernews.com/2026/09/mass-scanning-campaign-exploits-vite.html , https://cve-security.com/cve/cve-2026-39364
- **workerd — 5 vulnerabilities disclosed (2 Critical) by Check Point Research, presented at Black Hat USA 2026** — includes a zlib use-after-free and an HTMLRewriter use-after-free rated Critical by Cloudflare; creates sandbox-escape / cross-tenant exposure risk for self-hosted workerd. Cloudflare's managed environment is patched; self-hosted workerd should be on v1.20260619.1 or later. https://research.checkpoint.com/2026/when-agentic-glue-melts/
- **vee-validate / yup — no specific 2026 CVEs found**; search results instead note vee-validate's low recent release cadence (no npm release from the vee-validate ecosystem in the past 12 months per one source), which is a maintenance-activity signal rather than a vulnerability. https://socket.dev/npm/package/@vee-validate/yup
- **vue-router — no specific CVE found** for the September 2026 window in this search.

## Source: mctl metrics
- **mctl-web service status** — ArgoCD app `admins-mctl-web`: health=Healthy, syncStatus=Synced, imageTag=7.5.3, host=mctl.ru, port=80, componentType=base-service, updatedAt=2026-09-19T00:00:52Z.
- **Version/host discrepancy** — mctl_get_service_status/config report imageTag `7.5.3` and host `mctl.ru`, while context/current-version.md states version 4.6.2 and context/architecture.md documents the primary host as `mctl.ai` (mctl.ru listed only as a redirect source). Recording as-is, no interpretation.
- **admins tenant resource usage** — limits.cpu used 6750m/10 allocated, limits.memory used 3328Mi/5Gi allocated, requests.cpu 565m/2, requests.memory 1520Mi/3Gi, pods 7/12, services 8/10, PVCs 0/2.
- **labs tenant resource usage** (flagged platform-wide as memory-constrained) — limits.memory used 8736Mi/10.5Gi allocated (~83%), limits.cpu used 11300m/12 allocated (~94%), requests.memory 3792Mi/5Gi, requests.cpu 1935m/3, pods 16/25, services 17/20, PVCs 0/2.

## Source: mctl incidents
- **No open incidents** — mctl_list_incidents for service=mctl-web, team=admins returned count=0, items=[].

## Top-3 (for spec-writer)

No new Top-3 items today. Every substantive finding in this inbox is either (a) the same
underlying issue as an already-open proposal in `proposals/` (the correct action is to
refresh that existing proposal with today's new details, not open a near-duplicate slug),
or (b) out of architectural scope / unconfirmed for mctl-web's actual stack. Per the
analyst brief, fewer than 3 items are reported rather than padding with duplicates.

The single most urgent item to flag for follow-up (not a new proposal, but an update to
an existing one): **`nuxt-security-patch`** should be refreshed immediately with
CVE-2026-71316 (7.5 HIGH), CVE-2026-71320 (8.1 HIGH), and CVE-2026-71318 (4.8 MEDIUM) —
the two HIGH-severity CVEs affect the range that includes our pinned Nuxt 4.3.1, and are
fixed in 4.5.1+. This is dependency-tracking maintenance on an existing proposal, not a
fresh finding, so it is not listed as a numbered Top-3 item here.

## Dropped

**Duplicates of already-open proposals** (underlying issue already tracked; existing
proposal should be updated with today's specifics rather than a new slug being created):
- CVE-2026-39364 (Vite dev-server file read, actively exploited) — subsumed by `vite-dev-server-file-read-cve`.
- CVE-2026-71316 / CVE-2026-71320 / CVE-2026-71318 (Nuxt SSRF/injection/payload-disclosure CVEs) and the Nuxt v4.5.2 release — subsumed by `nuxt-security-patch`; recommend updating that proposal urgently given HIGH severity and applicability to pinned 4.3.1 (see note above).
- Vue core v3.5.43 release — subsumed by `vue-patch-3-5-33/34` (bump target).
- vue-router v5.3.1 fetch — subsumed by `vue-router-v5-migration`; also flagged as a low-confidence/stale data point (release date reported as 2024 on a 2026 "latest" query).
- @vueuse/core v15.0.0 release (breaking changes: dropped Node 20, removed templateRef support, `scheduler` param) — subsumed by `vueuse-upgrade-14-3`; that proposal's target/breaking-change review should be updated to account for v15 rather than v14.3.
- dart-sass 1.104.1 release — subsumed by `sass-upgrade-1-99` (target bump).
- cloudflare/workerd v1.20260918.1 release — subsumed by `workerd-runtime-upgrade`.
- Check Point Research workerd CVEs (2 Critical: zlib UAF, HTMLRewriter UAF) — overlaps `workerd-runtime-upgrade`; additionally **not applicable as a new proposal** since mctl-web's Worker runs on Cloudflare's managed Workers platform via Wrangler (per context/architecture.md), not self-hosted workerd, and the source explicitly states Cloudflare's managed environment is already patched.
- mctl-web imageTag/host discrepancy (7.5.3/mctl.ru vs current-version.md 4.6.2 / architecture.md mctl.ai) — subsumed by `reconcile-current-version-doc` and `mctl-ru-host-drift`.

**Low relevance / architectural fit** (not duplicates, but do not clear the bar):
- CVE-2026-61793 (nuxt-og-image SSRF) — module usage is unconfirmed and not referenced anywhere in context/architecture.md; per architectural-fit criterion, drop until confirmed in use.
- @cloudflare/containers-shared 0.17.0 (Wrangler container build support) — mctl-web's Cloudflare Worker is deployed via Wrangler for OAuth/API routes only; no container-build usage in the pipeline described in context/architecture.md, so this is out of scope.
- vee-validate v4.15.1 fetch — matches the currently pinned version exactly; no action.
- yup fetch (v1.0.0, inconsistent/stale vs. pinned 1.7.1) — data quality issue only, not actionable.
- admins tenant resource usage (~65% memory, ~67% CPU of limits) — healthy, no risk signal.
- labs tenant resource usage (~83% memory, ~94% CPU) — reconfirms the known platform constraint from CLAUDE.md; no mctl-web-specific action is triggered by today's numbers, and no proposal here would add load to `labs` (mctl-web runs in `admins`).
- No open incidents — informational only.
