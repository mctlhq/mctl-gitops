# Inbox 2026-09-26

## Source: github releases
- **Nuxt latest release unchanged since last check** — `nuxt/nuxt` latest is still v4.5.2 (published Aug 5), pinned baseline in context/architecture.md is 4.3.1. https://github.com/nuxt/nuxt/releases/latest
- **Vue core moved to v3.5.43** — `vuejs/core` latest release v3.5.43, published Sep 17 08:36 UTC; pinned baseline is 3.5.30. Release notes page had loading errors, could not extract bullet changelog; see https://github.com/vuejs/core/blob/main/CHANGELOG.md. https://github.com/vuejs/core/releases/latest
- **vue-router latest tag shows v5.3.1 — needs verification** — `vuejs/router` releases/latest resolved to v5.3.1 (published Sep 2, 2026, single-commit fix for non-string params in `isActive`), which is a jump from our pinned 4.6.4 (Vue 3 apps normally track router v4.x); flagging as possibly a different major line or a fetch artifact, worth double-checking manually. https://github.com/vuejs/router/releases/latest
- **vee-validate unchanged since last check** — `logaretm/vee-validate` latest is still v4.15.1 (published Jun 7): object-equality fix, Valibot intersection/variant fixes, Slovenian i18n strings added. Matches current pin. https://github.com/logaretm/vee-validate/releases/latest
- **yup releases/latest resolves to v1.0.0 — likely stale GitHub "latest" tag** — page shows v1.0.0 (Feb 8) titled "v1 because I finally got around to it"; our pin is yup 1.7.1, so GitHub's "latest" release marker is behind the actual npm-published version (yup ships without marking newer GitHub releases as "latest" in the past too). https://github.com/jquense/yup/releases/latest
- **@vueuse/core released v15.0.0** — published Sep 16, breaking changes: dropped `templateRef`, dropped Node 20 support, timer options replaced by "scheduler", useThrottleFn trailing default flipped to true, IndexedDB now syncs cross-tab; new `useWebMCP`, `useLiveAnnouncer`, `useTemporalNow`. Pinned baseline is 14.2.1. https://github.com/vueuse/vueuse/releases/latest
- **dart-sass released 1.105.0** — published Sep 22 17:33, adds first-class modules support (`meta.load()`, `meta.get-module()`), new `meta.css()` mixin, `SassModule` class + `Value.assertModule()` in JS/Dart APIs. Pinned baseline is sass 1.98.0. https://github.com/sass/dart-sass/releases/latest
- **cloudflare/workers-sdk latest tagged release is containers-shared@0.20.2** — published Sep 25 19:34, dependency-bump-only patch release (config 0.19.0, workers-utils 0.44.0, cli-shared-helpers 0.2.0, build-output-utils 0.8.1), no wrangler-core feature changes noted on this page. https://github.com/cloudflare/workers-sdk/releases/latest
- **cloudflare/workerd released v1.20260925.2** — published Sep 25 14:18 UTC: native stream close-during-handoff fix, tee() lock fix for closed/errored streams, Durable Objects configurable retries (STOR-5615), same-worker Workflow execution support, SQLite internal exception context. https://github.com/cloudflare/workerd/releases/latest

## Source: cve
- **CVE-2026-71316 / 71318 / 71320 (Nuxt) — already fixed in our version range** — payload-disclosure (7.5), island `as`-prop dynamic-component resolution (4.8), and island template-injection RCE-in-Nitro (8.1); all fixed in 3.21.10 / 4.5.1. Our pinned Nuxt is 4.3.1 (context/architecture.md) but the live deploy reports imageTag 7.5.3 (see mctl metrics section) — version reconciliation needed to confirm patch status. https://www.strix.ai/cve/CVE-2026-71320
- **CVE-2026-53721 (Nuxt) — incomplete fix noted** — route rules silently dropped for mixed-case paths, bypassing appMiddleware auth gates; search result described the existing fix as incomplete, no further patched-version detail found this pass.
- **CVE-2026-39364 (Vite dev server) — still under active mass exploitation** — F5 Labs recorded 32,000+ attack events in August 2026 (19x prior baseline) against exposed dev servers bypassing `server.fs.deny` to read `.env`/cloud credentials; fixed in Vite 7.3.2 / 8.0.5. Relevant if any Nuxt dev server is ever exposed publicly. https://securityonline.info/vite-cve-2026-39364-exploited/
- **CVE-2026-0933 (Cloudflare Wrangler) — critical command injection, CVSS 9.9** — `wrangler pages deploy --commit-hash` interpolates the value unsanitized into a shell command, allowing arbitrary command execution in CI/CD (token/secret exposure risk); affects wrangler >=2.0.15 <3.114.17 and >=4.0.0 <4.59.1, fixed in 3.114.17 / 4.59.1. Our deploy.yml uses Wrangler via GitHub Actions per context/architecture.md — version currently deployed not confirmed this pass. https://github.com/cloudflare/workers-sdk/security/advisories/GHSA-36p8-mvp6-cv38
- **No new Vue core CVEs found** — searches turned up only the pre-existing low-severity ReDoS-in-parseHTML issue and unrelated third-party (Krayin CRM, vue-i18n) findings; nothing dated within the last 7-14 days against vuejs/core itself.
- **No vue-router-specific CVE found** — search returned only generic/old NVD and OpenCVE listing pages, no concrete 2026 vue-router advisory identified.
- **No vee-validate / yup specific CVE found** — search surfaced only the unrelated CVE-2026-0775 (npm CLI local privilege escalation) and a note that vee-validate has had no new npm releases in ~12 months per Snyk.
- **No @vueuse/core CVE found** — no advisories located; package scored clean on a third-party scanner (secure.software).
- **dart-sass itself has no direct CVE** — one dependency-level finding: CVE-2026-93749 in `source-map-js` 1.2.0 (severity 7.5), which dart-sass may pull transitively; worth checking our lockfile's source-map-js version.
- **No workerd-specific CVE found in this pass** — general September 2026 CVE roundups (SAP, Check Point, F5 BIG-IP, Cisco, Microsoft Patch Tuesday) returned nothing naming `workerd`; the previously-known workerd zlib/HTMLRewriter UAF (Check Point Research) was not freshly reconfirmed or updated in this search.

## Source: mctl metrics
- **mctl-web service status: Healthy / Synced** — ArgoCD app `admins-mctl-web`, health Healthy, syncStatus Synced, last updatedAt 2026-09-26T00:00:28Z.
- **Deployed imageTag is 7.5.3, host mctl.ru — both diverge from local context** — `mctl_get_service_status`/`mctl_get_service_config` report imageTag "7.5.3" and host "mctl.ru", whereas context/current-version.md records version 4.6.2 (as of 2026-04-25) and context/architecture.md describes mctl.ai as primary with mctl.ru only as a redirect source. Raw discrepancy, not further investigated here.
- **admins tenant resource usage** — CPU: 300m requests / 1650m limits used against quota requests.cpu=2, limits.cpu=10; memory: 704Mi requests / 1536Mi limits used against quota requests.memory=3Gi, limits.memory=5Gi; pods 7/12; services 9/10 (near services quota).
- **labs tenant resource usage** — CPU: 2285m requests / 13250m limits used against quota requests.cpu=3, limits.cpu=16; memory: 4368Mi requests / 10016Mi limits used against quota requests.memory=6Gi, limits.memory=14Gi (~71% of memory limit allocation used); pods 19/30; services 20/25.

## Source: mctl incidents
- **No open incidents for mctl-web** — `mctl_list_incidents(service=mctl-web)` returned count=0, items=[].

## Top-3 (for spec-writer)

> **Correction to the analyst's duplicate-check (post-hoc, by the run orchestrator):** the analyst's `Glob` of `proposals/**`/`proposals/*` incorrectly returned no files, leading it to conclude the directory doesn't exist. It does exist and is extensively populated (40+ existing proposal slugs, verified directly via `ls`/`Glob` after the fact). Items #1 and #2 originally selected by the analyst were re-checked against that directory's actual contents and found to be **duplicates of existing proposals** — they have been moved to Dropped below with the specific slugs they duplicate. Only the genuinely new item (source-map-js CVE) remains as Top-3 #1. This keeps the Top-3 section accurate for spec-writer, which is instructed to append `-v2` on slug collision rather than skip — so passing known duplicates through would have produced redundant `-v2` proposals.

### 1. sass-transitive-source-map-js-cve-check: Verify/patch transitive source-map-js CVE via dart-sass
**Category:** security
**Impact:** 3
**Effort:** 1
**Rationale:** CVE-2026-93749 (severity 7.5) affects `source-map-js` 1.2.0, a dependency dart-sass (which we use for styles, pinned 1.98.0) may pull in transitively; a lockfile check plus a `sass` bump if needed is a very low-effort way to close a potential supply-chain gap. Kept at impact 3 (not higher) because applicability to our lockfile is unconfirmed and source-map-js is normally only a build-time dependency, not runtime-exposed. Verified via `Grep` against `proposals/` that no existing proposal (including `sass-deprecation-fix` and `sass-upgrade-1-99`, which target the unrelated 1.99.0 deprecation-warning/parent-selector changes) covers this specific CVE — this is genuinely new.
**Source:** CVE-2026-93749 (researcher CVE pass, 2026-09-26)

## Dropped
- **wrangler-cve-2026-0933-command-injection (CVE-2026-0933, CVSS 9.9, Wrangler `pages deploy --commit-hash` shell injection)** — **duplicate**, confirmed already covered by three existing proposals: `proposals/wrangler-cve-0933/`, `proposals/wrangler-cve-2026-0933/`, and `proposals/wrangler-cve-command-injection/`, all of which already specify EARS acceptance criteria requiring wrangler >= 4.59.1 in `deploy.yml`. No new proposal created; the correct action (out of scope for this cycle's spec-writer step) would be to check whether the live deployed wrangler version has actually been bumped yet and refresh one of those three existing proposals' status if still open.
- **reconcile-deployed-version-and-host-drift (imageTag `7.5.3`/host `mctl.ru` vs. docs)** — **duplicate**, confirmed already covered by two existing proposals: `proposals/reconcile-current-version-doc/` (version-drift half) and `proposals/mctl-ru-host-drift/` (host-drift half). No new proposal created.
- **CVE-2026-53721 (Nuxt route-rules/appMiddleware auth bypass)** — poor architectural fit: our Nuxt routes (`/`, `/docs`, `/privacy`) are static/prerendered with no `appMiddleware`-based auth; real auth (GitHub OAuth) is handled entirely by the Cloudflare Worker, not Nuxt route rules. Not applicable to our deployment.
- **CVE-2026-39364 (Vite dev server mass exploitation)** — only exploitable if a Vite/Nuxt dev server is exposed publicly; our production path is `nuxt build` → static `dist/` served via Cloudflare Pages/nginx, no public dev server per `context/architecture.md`. Worth a dev-hygiene reminder, not a platform proposal.
- **Nuxt CVE-2026-71316/71318/71320 patch status** — folded into Top-3 item #2 (reconcile-deployed-version-and-host-drift), since confirming the patch requires first resolving which Nuxt version is actually running in production.
- **vue-router "v5.3.1" release note** — researcher itself flagged this as possibly a fetch artifact / wrong major line (Vue 3 apps track router v4.x, we're pinned to 4.6.4); not actionable until re-verified.
- **@vueuse/core v15.0.0 breaking-change upgrade** — real dependency, real breaking changes (drops `templateRef`, drops Node 20 support), but it's a non-security DX/refactor item with real migration cost; lower priority than this cycle's security items. Revisit next cycle if no higher-priority security findings appear.
- **dart-sass 1.105.0 (new module features)** — nice-to-have feature release, no fix or urgency behind it.
- **Nuxt/vee-validate "unchanged" and yup "stale latest tag" items** — no new signal.
- **cloudflare/workerd v1.20260925.2 and workers-sdk containers-shared@0.20.2 releases** — dependency-bump/internal-fix releases with no confirmed applicability to features our Worker (`/api/github/login`, `/api/submit`, `/api/contact`) actually uses.
- **No open incidents** — nothing to act on.
- **labs tenant at ~71% of memory limit** — noted per platform constraint, but none of the Top-3 items above touch the `labs` tenant (mctl-web runs in `admins`); no risk flag needed this cycle.
