# Tasks: vue-patch-3-5-33

- [ ] 1. Проверить, объявлен ли `vue` явно в `package.json` — DoD: установлено, является ли `vue` явной зависимостью или только транзитивной; зафиксирован текущий способ управления версией.
- [ ] 2. Обновить версию Vue до `^3.5.33` — DoD: `package.json` содержит `"vue": "^3.5.33"` (явно) или `npm update vue@3.5.33` обновил lock-файл; `npm install` завершается без ошибок.
- [ ] 3. Запустить `nuxt build` и убедиться в отсутствии ошибок — DoD: `nuxt build` завершается с кодом 0; директория `dist/` содержит prerender-файлы для `/`, `/docs`, `/privacy` (зависит от 2).
- [ ] 4. Открыть PR с изменениями `package.json` и lock-файла — DoD: PR содержит только изменение версии Vue; CI-checks зелёные; PR одобрен (зависит от 3).
- [ ] 5. Смёрджить PR в main — DoD: коммит в main, production-деплой успешен (зависит от 4).

## Тесты

- [ ] T1. `node_modules/vue/package.json` содержит `"version": "3.5.33"` (или выше в 3.5.x) после `npm install`.
- [ ] T2. `nuxt build` завершается с кодом 0 без новых предупреждений.
- [ ] T3. Prerender-файлы `dist/index.html`, `dist/docs/index.html`, `dist/privacy/index.html` созданы и не пустые.
- [ ] T4. Браузерный smoke-тест: открыть `/`, `/docs`, `/privacy` — нет ошибок в консоли браузера, нет визуальных регрессий.

## Откат
1. Вернуть предыдущую версию Vue в `package.json` (например, `^3.5.30`).
2. Запустить `npm install` для восстановления lock-файла.
3. Если изменения задеплоены — использовать Cloudflare Dashboard для rollback к предыдущему Pages deployment.
4. Смёрджить hotfix-ветку в main для восстановления CI-пайплайна.

> Примечание: этот откат тривиален, поскольку изменение минимально. При patch-обновлениях Vue откат маловероятен, но задокументирован для полноты.
