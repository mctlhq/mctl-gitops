# Inbox 2026-04-29 (mctl-docs sibling-repo scan)

> Run: 2026-04-29. Mode: **Mode B — GitHub API fallback** (sibling repos absent at
> `SIBLING_REPOS_PATH=/Users/dmitriimashkov/PycharmProjects/mctlhq`).
> `GITHUB_TOKEN` not set; unauthenticated public API used (60 req/h cap).
> Rate-limit remaining at scan start: 60/60.  
> `mcp__mctl__*` tools: unavailable — version-status: **unverified** for all entries.

---

## Sibling repos — git log scan (since 2026-04-22)

### Repo: mctl-api (prod-version: unverified)
**no signal: mctl-api — GitHub API returned 404 (private repo, GITHUB_TOKEN not set)**

### Repo: mctl-web (prod-version: unverified)
**no signal: mctl-web — GitHub API returned 404 (private repo, GITHUB_TOKEN not set)**

### Repo: mctl-portal (prod-version: unverified)
**no signal: mctl-portal — GitHub API returned 404 (private repo, GITHUB_TOKEN not set)**

### Repo: mctl-agent (prod-version: unverified)
**no signal: mctl-agent — GitHub API returned 404 (private repo, GITHUB_TOKEN not set)**

### Repo: mctl-agents (prod-version: unverified)
**no signal: mctl-agents — GitHub API returned 404 (private repo, GITHUB_TOKEN not set)**

### Repo: mctl-gitops (prod-version: unverified)
**no signal: mctl-gitops — GitHub API returned 404 (private repo, GITHUB_TOKEN not set)**

---

### Repo: mctl-openclaw (prod-version: unverified)

> Source: `https://api.github.com/repos/mctlhq/mctl-openclaw/commits?since=2026-04-22T00:00:00Z&per_page=100`
> Total non-merge commits returned: 96. Paginated: 1 page (LINK_NEXT: no). Sanity cap: not hit.
> Conventional-commit filter applied below; CI/test/chore/build/style/refactor dropped.

---

#### Commit `c2d31a5` — fix(outbound): strip internal runtime scaffolding

- **Date:** 2026-04-28
- **Type:** fix (security)
- **User-visible effect:** Outbound messages delivered to all channels (Discord, Telegram, etc.) now have `<system-reminder>` and `<previous_response>` XML tags stripped at the final delivery boundary, so degraded or plugin-injected harness replies cannot leak internal scaffolding to end users.
- **Diff highlight:**
  ```
  // discord/src/outbound-adapter.ts
  const DISCORD_INTERNAL_RUNTIME_SCAFFOLDING_BLOCK_RE =
    /<\s*(system-reminder|previous_response)\b[^>]*>[\s\S]*?<\s*\/\s*\1\s*>/gi;
  // …
  sanitizeText: ({ text }) => stripDiscordInternalRuntimeScaffolding(text),
  ```
  Core delivery layer test: verifies `<previous_response>null</previous_response>visible` → `"visible"`.
- **Docs:** gap
- **Suggested doc location:** `docs/platform/openclaw.md` — new Security section

---

#### Commit `c5c08c0` — fix(agents): mark inter-session prompts

- **Date:** 2026-04-28
- **Type:** fix (security / behaviour change)
- **User-visible effect:** Agent-to-agent (`sessions_send`) and A2A reply prompts now carry an `[Inter-session message … isUser=false]` envelope so the receiving model explicitly knows the input is not a live end-user instruction. Previously only transcript provenance metadata was set; now the inline text is also marked.
- **Docs:** gap
- **Suggested doc location:** `docs/platform/openclaw.md` — Security / agent isolation section

---

#### Commit `1e9faa2` — docs: document inter-session prompt guards

- **Date:** 2026-04-28
- **Type:** docs (new concept)
- **User-visible effect:** OpenClaw's own `docs/concepts/session-tool.md` was updated to explain the new `isUser=false` envelope. mctl-docs (`docs.mctl.ai`) does not yet reflect this.
- **Docs:** gap (openclaw internal docs updated; docs.mctl.ai/platform/openclaw.md not yet)
- **Suggested doc location:** `docs/platform/openclaw.md`

---

#### Commit `98f5fd1` — docs(gateway/security): list system-reminder and previous_response in outbound stripping

- **Date:** 2026-04-28
- **Type:** docs (new concept)
- **User-visible effect:** OpenClaw's own gateway security reference now explicitly names `<system-reminder>` and `<previous_response>` as stripped tags. mctl-docs does not surface this guarantee.
- **Docs:** gap
- **Suggested doc location:** `docs/platform/openclaw.md`

---

#### Commit `490e6d6` — feat(docker): add OPENCLAW_SKIP_ONBOARDING env to skip onboarding during Docker setup (#55518)

- **Date:** 2026-04-28
- **Type:** feat
- **User-visible effect:** Operators running automated Docker installs (e.g. via CI, ArgoCD `hook`-style jobs, or pre-configured images) can now set `OPENCLAW_SKIP_ONBOARDING=1` to bypass the interactive onboarding step while still applying gateway defaults. Directly relevant to mctl's gitops-based deployment model.
- **Diff highlight:**
  ```
  # docs/install/docker.md (openclaw's own docs)
  | `OPENCLAW_SKIP_ONBOARDING` | Skip interactive onboarding (`1`,`true`,`yes`,`on`) |
  ```
- **Docs:** gap
- **Suggested doc location:** `docs/platform/openclaw.md` — Deployment / setup env vars section

---

#### Commit `652bde3` — podman: wire OPENCLAW_INSTALL_BROWSER build-arg to setup script (#63407)

- **Date:** 2026-04-28
- **Type:** feat (Podman build enhancement)
- **User-visible effect:** Podman-based installs can now pass `OPENCLAW_INSTALL_BROWSER` as a build argument to control browser installation during image build. Minor relevance to mctl docs unless Podman is in use.
- **Docs:** gap (minor)
- **Suggested doc location:** `docs/platform/openclaw.md` — if Podman deployment is covered

---

#### Commit `bcc6a24` — fix(gateway): make handshake timeout configurable

- **Date:** 2026-04-28
- **Type:** fix / new config knob
- **User-visible effect:** A new `gateway.handshakeTimeoutMs` option (default `15000` ms) can be set in OpenClaw config to give local clients more time to complete the pre-auth WebSocket handshake on loaded or low-powered hosts. `OPENCLAW_HANDSHAKE_TIMEOUT_MS` env var still takes precedence.
- **Diff highlight:**
  ```
  # docs/gateway/configuration-reference.md (openclaw docs)
  - `gateway.handshakeTimeoutMs`: pre-auth WebSocket handshake timeout in ms.
    Default: 15000. OPENCLAW_HANDSHAKE_TIMEOUT_MS takes precedence.
  ```
- **Docs:** gap
- **Suggested doc location:** `docs/platform/openclaw.md` — Configuration reference / troubleshooting

---

#### Commit `771846c` — fix(bedrock): omit Opus temperature for profiles

- **Date:** 2026-04-28
- **Type:** fix
- **User-visible effect:** Bedrock Opus 4.7 profiles no longer send a temperature parameter (Anthropic's Bedrock endpoint rejects it). Agent responses using Bedrock/Opus profiles will no longer fail with a provider error. Already documented in openclaw's own `docs/providers/bedrock.md` via commit `14f140d`.
- **Docs:** gap (for docs.mctl.ai if it references Bedrock as a supported provider)
- **Suggested doc location:** `docs/platform/openclaw.md` — if Bedrock provider listing is present

---

#### Commits `b1195c6`, `07089f1`, `6ade320`, `4bd3d25`, `9f37ff0`, `5b1202e` — BlueBubbles routing-guard hardening

- **Date:** 2026-04-22 to 2026-04-28
- **Type:** fix (channel routing)
- **User-visible effect:** Multiple fixes to BlueBubbles channel routing: DM vs group `chat_guid` disambiguation, dropping reactions without chat identifiers, scoping short message-id resolution to the caller's chat. Users relying on BlueBubbles DM routing previously saw messages mis-routed to group channels.
- **Docs:** gap (minor — if BlueBubbles is listed as a supported channel in docs.mctl.ai)
- **Suggested doc location:** `docs/platform/openclaw.md` — channels section, or drop (bugfix only)

---

## Summary

- Total commits scanned (git log / API): 96 (mctl-openclaw only)
- Repos with signal: 1 (mctl-openclaw via GitHub API public endpoint)
- Repos with no signal (private/404): 6 (mctl-api, mctl-web, mctl-portal, mctl-agent, mctl-agents, mctl-gitops)
- Conventional-commit filter: kept 12 significant commits, dropped ~84 (test/ci/chore/build/style/refactor/perf)
- gap: 8 (grouped into 4 themes: outbound security, docker setup, gateway config, BlueBubbles routing)
- stale: 0
- documented: 0
- in-flight (do NOT propose): 0
- version-status: unverified (mcp__mctl__* unavailable; GITHUB_TOKEN not set)

---

## Top-3 (for spec-writer)

> Analyst run: 2026-04-29.
> ADR cross-check: `0001-vitepress-stack.md` — no constraints triggered (no stack change proposals).
> Existing proposals checked: `mermaid-dep-security`, `vitepress-upgrade-strategy`, `mermaid-diagrams-guide` — no overlap with openclaw topics.
> All three items below are new gaps not covered by any existing proposal.

---

### 1. openclaw-outbound-security: Document OpenClaw outbound sanitization and inter-session isolation

**Repo(s):** mctl-openclaw  
**Affected commit(s):** `c2d31a5`, `c5c08c0`, `1e9faa2`, `98f5fd1`  
**Category:** update-page  
**User-visible impact:** 4/5  
**Doc complexity (effort):** 2/5  
**Suggested doc location:** `docs/platform/openclaw.md` (add a new "Security" section)  
**Rationale:** Four commits shipped a meaningful security guarantee: outbound messages delivered over any OpenClaw channel now have internal scaffolding (`<system-reminder>`, `<previous_response>`) stripped at the delivery boundary, and agent-to-agent messages are explicitly marked `isUser=false`. mctl tenants route real user-facing messages through OpenClaw; knowing this guarantee exists (and what it covers) is directly relevant. OpenClaw's own docs were updated; `docs.mctl.ai/platform/openclaw.md` has not been. This is a stale-vs-gap situation where the external doc was updated but the mctl integration page was not.

---

### 2. openclaw-docker-skip-onboarding: Document OPENCLAW_SKIP_ONBOARDING for automated deployments

**Repo(s):** mctl-openclaw  
**Affected commit(s):** `490e6d6`  
**Category:** update-page  
**User-visible impact:** 3/5  
**Doc complexity (effort):** 1/5  
**Suggested doc location:** `docs/platform/openclaw.md` (add to a "Deployment configuration" / env vars table)  
**Rationale:** The mctl platform deploys OpenClaw via ArgoCD/GitOps — automated, non-interactive. `OPENCLAW_SKIP_ONBOARDING=1` is the official way to skip the interactive onboarding step in that context. Without this documented on docs.mctl.ai, a platform operator setting up a new tenant would need to hunt through openclaw's own Docker guide to find the flag. Low-effort, high-utility.

---

### 3. openclaw-gateway-handshake-timeout: Document gateway.handshakeTimeoutMs configuration

**Repo(s):** mctl-openclaw  
**Affected commit(s):** `bcc6a24`  
**Category:** update-page  
**User-visible impact:** 2/5  
**Doc complexity (effort):** 1/5  
**Suggested doc location:** `docs/platform/openclaw.md` (add to a "Configuration reference" or "Troubleshooting" section)  
**Rationale:** Loaded or resource-constrained Kubernetes nodes (which the mctl platform runs on) can fail WebSocket handshakes under startup pressure. The new `gateway.handshakeTimeoutMs` knob (default 15 s) is the correct operational lever. It will appear in the reference config tables openclaw ships; docs.mctl.ai should surface it so mctl admins find it without reading the upstream config reference in full.

---

## Dropped

- **BlueBubbles routing hardening** (`b1195c6`, `07089f1`, `6ade320`, `4bd3d25`, `9f37ff0`, `5b1202e`) — pure bugfix series; no new configuration or user-action required; routing behaviour now matches what was always intended. Not a doc gap.
- **Bedrock Opus 4.7 temperature** (`771846c`, `14f140d`) — already documented in openclaw's own `docs/providers/bedrock.md`. docs.mctl.ai does not currently have a providers reference; adding one for a single temperature quirk is out of scope.
- **Podman OPENCLAW_INSTALL_BROWSER** (`652bde3`) — Podman build arg, no evidence mctl platform uses Podman (ArgoCD + Docker is the documented stack). Low signal-to-noise.
- **All 6 private repos (mctl-api, mctl-web, mctl-portal, mctl-agent, mctl-agents, mctl-gitops)** — infrastructure issue (private repos, no GITHUB_TOKEN); not a doc gap. Recommend setting GITHUB_TOKEN in agent environment for future runs.
