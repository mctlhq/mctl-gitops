# Inbox 2026-09-19 (mctl-docs sibling-repo scan)

Scan window: `since=2026-09-12T00:00:00Z` (7 days back from 2026-09-19), Mode B
(GitHub API, authenticated, `SIBLING_REPOS_PATH` does not exist in this
environment). `mctl-gitops` was scanned via Mode A (local clone at
`/workdir/mctl-gitops` — its `origin` remote matches `mctlhq/mctl-gitops`).

Note: this is the first scan since 2026-09-05 (a ~2-week gap in the inbox
history), but per the skill's default this run still only covers the
standard 7-day window, not the full gap. If the analyst wants the missing
2026-09-06..2026-09-11 window covered, that needs a separate backfill run.

## Repo: mctl-api  (prod-version: unverified — mctl-api is not present in
`mctl_list_services`; it is likely the core platform API deployed outside
the GitOps service registry this tool enumerates)

Volume note: 102 non-merge commits in the window. Most are an in-progress,
multi-day hardening pass on a new "lifecycle ownership store" feature
(ADR-009/010: `internal/lifecycle/...`) — dozens of `fix(lifecycle):` /
`test(lifecycle):` commits closing races, CAS bugs, and edge cases in that
one store. Below are the commits that introduce user/operator-visible
surface; the rest of the `fix(lifecycle)` cluster is internal correctness
work on the same feature and is not listed individually.

### Commit 5a1da2d — feat(lifecycle): add the lifecycle ownership store
- **Date:** 2026-09-12
- **Type:** feat
- **User-visible effect:** Introduces a new backing store that tracks who
  (which agent/workflow) currently owns a given lifecycle phase of an
  entity (proposal, PR). Foundational for the two externally-reachable
  commits below.
- **Docs:** gap
- **Suggested doc location:** new page, e.g. `docs/platform/lifecycle-ownership.md`, linked from `docs/platform/architecture.md`

### Commit e7aee63 — feat(lifecycle): expose ownership over HTTP with an explicit auth boundary
- **Date:** 2026-09-12
- **Type:** feat
- **User-visible effect:** Adds new REST HTTP endpoints (`internal/api/handlers_lifecycle.go`) that let a caller read/write lifecycle-ownership records. Confirmed live in the `docs/api` surface (`internal/openapi/openapi.yaml` touched by a related commit).
- **Docs:** gap
- **Suggested doc location:** `docs/api/index.md` (new endpoint section)

### Commit 1064b2c — feat(lifecycle): add mctl_get_lifecycle_ownership
- **Date:** 2026-09-14
- **Type:** feat
- **User-visible effect:** Adds a brand-new MCP tool, `mctl_get_lifecycle_ownership`, that lets an MCP client (e.g. this agent) read the lifecycle-ownership store directly. This tool is present in this agent's own tool list today, so it is confirmed live.
- **Docs:** gap
- **Suggested doc location:** `docs/mcp/tools-reference.md` (new tool entry), `docs/mcp/examples.md` (usage example)

### Commit 783a238 — feat(lifecycle): reject the deprecated ?id on the list path
- **Date:** 2026-09-14
- **Type:** feat (deprecation-removal, user-visible: `?id` on the lifecycle list endpoint now returns 400 instead of silently delegating)
- **User-visible effect:** Any caller still using the deprecated `?id` query param against the lifecycle-ownership list endpoint now gets a 400 pointing at `/record` instead of a silent delegate.
- **Docs:** gap (same undocumented endpoint family as e7aee63 above — nothing describes the deprecated param today, so nothing goes stale, but the eventual doc page should state the current contract directly)
- **Suggested doc location:** `docs/api/index.md`

### Commit f95ce9b — feat(mcp): dispatch the portal server-auth apply without holding the token
- **Date:** 2026-09-12
- **Type:** feat
- **User-visible effect:** New MCP tool for dispatching the Cloudflare MCP portal's OAuth-registration apply workflow (`cloudflare-apply.yml`) without the caller holding a GitHub token. Confirmed live — this agent has the resulting tool (`mctl_trigger_portal_server_auth_apply`) today.
- **Docs:** gap
- **Suggested doc location:** `docs/mcp/tools-reference.md`

### Commit 3b68ce3 — feat(portal): expose every api tool on the aggregate, mctl's own checks decide
- **Date:** 2026-09-12
- **Type:** feat
- **User-visible effect:** Widens the Backstage/portal MCP aggregate allowlist so every mctl-api tool is reachable through the portal aggregate (previously a curated subset); safety is now enforced by mctl-api's own checks instead of the allowlist.
- **Docs:** gap
- **Suggested doc location:** `docs/mcp/overview.md` or `docs/mcp/tools-reference.md` (note on portal-aggregate vs direct-connection tool availability)

### Commit 19d1d22 — feat(events): GitHub pull request webhook producer
- **Date:** 2026-09-17
- **Type:** feat
- **User-visible effect:** New GitHub PR webhook receiver (`internal/api/handlers_github_webhook.go`) plus a new outbox/relay event pipeline (Valkey-backed) that turns incoming GitHub webhook deliveries into internal events. Likely feeds the lifecycle-reconcile / DevLoop machinery rather than being a directly user-configured integration, but it is a new externally-reachable HTTP endpoint.
- **Docs:** gap (moderate confidence — needs author/maintainer confirmation on whether this endpoint is meant to be publicly documented or stays an internal integration detail)
- **Suggested doc location:** `docs/platform/architecture.md` (component diagram) if it is meant to be documented; otherwise mark out of scope

### Commits 3a88f82, ea2194b, da6192d — fix(oauth) cluster: client-registration state, echoed client fields, refresh rotation grace window
- **Date:** 2026-09-18
- **Type:** fix
- **User-visible effect:** Three related fixes to the OAuth dynamic-client-registration and refresh-token flow used by the Cloudflare MCP portal: client-registration state no longer piggybacks on `client_name`, echoed client fields are now bounded, and the refresh-rotation grace window was widened (previously could reject a legitimate near-window refresh).
- **Docs:** possibly stale — `docs/mcp/connecting.md` and/or `docs/security/authentication.md` may describe the OAuth flow at a level of detail affected by the grace-window change; cannot confirm from the title-only `docs-tree.md` snapshot, needs a content read of those two pages
- **Suggested doc location:** `docs/mcp/connecting.md`, `docs/security/authentication.md`

### Other mctl-api commits this window (not individually listed)
- ~80 further `fix(lifecycle):` / `test(lifecycle):` commits (2026-09-12 through 2026-09-13) hardening the same lifecycle-ownership store above (CAS races, heartbeat/liveness split, epoch fencing, error-classification correctness). Internal correctness work on an already-listed feature — no additional doc surface beyond the entries above.
- `fix(events):` cluster (fd9f0ec, d37b9dd, d5c0c85, 94ee4ac, 48813f4, 316cef4, c85079c, 4ffd3cf) — internal reliability hardening of the new webhook/outbox/relay pipeline from 19d1d22 above (single-publisher guarantee, bounded DB/Valkey calls, partial-batch guards). No separate doc surface.
- `fix(auth):` 6f7b96a / 5fb50ea — internal GitOps-clone credential handling (read-per-use, unreadable token mount fix). Not directly user-visible; skipped.
- `fix(gitops):` cluster (a665c31, 26cec99, 325dfeb, f6e66c4, dcd00c4) — internal fix so a credential never survives inside a cloned `.git` directory. Security-hardening, not a new capability; skipped as no doc surface.
- `docs:` commits (fec8d00, 073ee68, 5e0c1fee/5e0c1fe, 874e309, 7b58b3e) — in-repo code-comment corrections, not concept documentation for docs.mctl.ai; skipped per filter rules.
- `chore(main): release X.Y.Z` tags — skipped (internal).

## Repo: mctl-web  (prod-version: 7.5.3, per mctl_list_services)
- No commits in the 7-day window (confirmed via HTTP 200 + empty array, not a rate-limit/auth error).

## Repo: mctl-portal  (prod-version: unverified — not present in mctl_list_services under this name)
- No commits in the 7-day window (confirmed via HTTP 200 + empty array).

## Repo: mctl-agent  (prod-version: unverified — not present in mctl_list_services; likely runs as a controller, not a GitOps-tracked service)

### Commit 5171d37 — feat(monitor): classify ContainerOOMKilled as a resource-limit alert
- **Date:** 2026-09-12
- **Type:** feat
- **User-visible effect:** The self-healing agent now classifies `ContainerOOMKilled` AlertManager alerts as `TypeResourceLimit` (paired with a matching alert rule added in mctl-gitops#1220). Operators will see a new, named incident type for pod-level OOM kills instead of an unclassified/unalerted event.
- **Docs:** gap
- **Suggested doc location:** `docs/reference/troubleshooting.md`, `docs/platform/components.md` (mctl-agent block)

### Commit ffedcbe — feat(monitor): classify NodeMemoryHeadroomLow as a resource-limit alert
- **Date:** 2026-09-12
- **Type:** feat
- **User-visible effect:** Adds a second new alert classification, `NodeMemoryHeadroomLow` -> `TypeResourceLimit`, so node-level memory-pressure alerts now route through the same remediation-matching path as other resource-limit incidents.
- **Docs:** gap
- **Suggested doc location:** `docs/reference/troubleshooting.md`, `docs/platform/components.md` (mctl-agent block)

## Repo: mctl-agents  (prod-version: 1.46.0, per mctl_list_services / mctl_get_service_config for `mctl-agents-worker`)

Per `context/docs-tree.md`'s own mapping hint, **mctl-agents is explicitly
out of scope for docs.mctl.ai** ("NOT documented in docs.mctl.ai (internal
tool)"). 127 non-merge commits in the window, almost entirely the same
ADR-009/010 "lifecycle ownership" rollout seen on the mctl-api side
(`ExecutionClaim`, epoch fencing, handoff, shepherd/implementer wait-loop
hardening), plus shepherd merge-safety fixes. All internal to this agent's
own control plane.

Two facts worth flagging to the analyst even though no doc page applies:
- The release commit `91793a2` (2026-09-14, "release 1.46.0") is the last
  one matching the currently deployed `mctl-agents-worker` image tag
  (1.46.0). **Every commit from `b64b35b` (2026-09-18) onward — the entire
  `ExecutionClaim`/lifecycle-reconcile/shepherd-cadence cluster — is
  ahead of production** and tagged **in-flight, do not document yet**.
- ADR-009 / ADR-010 (`docs(adr):` commits a299424, 4ffda69, 627f52c,
  932c4f5, b36dcda, 3be4301) are internal architecture-decision records
  living in `mctl-gitops`/`mctl-agents`, not `docs.mctl.ai` pages — no
  action needed here, noted only for completeness.

No individual commit entries recorded for this repo (out of docs.mctl.ai
scope per the mapping hints; researcher agenda is to flag the fact, not
list ~127 internal commits).

## Repo: mctl-gitops  (prod-version: n/a — GitOps source-of-truth repo, scanned via local clone at `/workdir/mctl-gitops`, Mode A)

### Commit cf7c61d — chore(agents): investigate https://github.com/mctlhq/mctl-agents/issues/196
- **Date:** 2026-09-18
- **Type:** chore (auto-generated by the investigator agent proposal flow)
- **User-visible effect:** none — this is an automated proposal-state commit, not a platform change.
- **Docs:** documented (n/a — filtered per `chore:` rule)

Only 1 non-merge commit in the window and it is a `chore:` — no
actionable signal for mctl-gitops this week.

## Repo: mctl-openclaw  (prod-version: 2026.7.11-beta.2, per mctl_list_services, multiple tenants)
- No commits in the 7-day window (confirmed via HTTP 200 + empty array).

## Summary
- Total commits scanned: 234 (mctl-api 102, mctl-web 0, mctl-portal 0, mctl-agent 2, mctl-agents 127, mctl-gitops 1, mctl-openclaw 0)
- gap: 9 (mctl-api: 5a1da2d, e7aee63, 1064b2c, 783a238, f95ce9b, 3b68ce3, 19d1d22 [moderate confidence]; mctl-agent: 5171d37, ffedcbe)
- stale: 1 (mctl-api OAuth cluster: 3a88f82 / ea2194b / da6192d — needs a doc-content read to confirm, currently "possibly stale")
- documented: 1 (mctl-gitops cf7c61d, trivially — filtered chore, no doc need)
- in-flight (do NOT propose): ~80+ (all mctl-agents commits from 2026-09-18, i.e. b64b35b onward — ahead of the deployed 1.46.0 image; also out-of-scope for docs.mctl.ai regardless per the repo-level scope note above)
- no signal (empty window, not an error): mctl-web, mctl-portal, mctl-openclaw

## Top-3 (for spec-writer)

### 1. lifecycle-ownership: Document the new lifecycle-ownership store (concept, REST API, MCP tool)
**Repo(s):** mctl-api
**Affected commit(s):** 5a1da2d, e7aee63, 1064b2c, 783a238
**Category:** new-page
**User-visible impact:** 5
**Doc complexity (effort):** 4
**Suggested doc location:** new page `docs/platform/lifecycle-ownership.md` (concept: what a lifecycle phase/owner is, linked from `docs/platform/architecture.md`); update `docs/api/index.md` (new HTTP endpoints in `handlers_lifecycle.go`, including the `?id` deprecation now returning 400); update `docs/mcp/tools-reference.md` + `docs/mcp/examples.md` (new `mctl_get_lifecycle_ownership` tool).
**Rationale:** Four commits over 2026-09-12..09-14 add one coherent, already-shipped feature: a new ownership-tracking store, a new REST endpoint family with an explicit auth boundary, a new MCP tool this agent itself can call today, and a hardened deprecation on the old `?id` param. Nothing on docs.mctl.ai describes any of this — it's a whole new API/MCP surface with no page at all, the highest-impact gap this week. The ~80 accompanying `fix(lifecycle)` commits are internal correctness hardening of this same feature and add no extra doc surface, so this stays one gap, not five.

### 2. mcp-portal-tool-exposure: Document the widened MCP-portal tool surface and the new server-auth-apply tool
**Repo(s):** mctl-api
**Affected commit(s):** f95ce9b, 3b68ce3
**Category:** update-page
**User-visible impact:** 4
**Doc complexity (effort):** 2
**Suggested doc location:** `docs/mcp/tools-reference.md` (add `mctl_trigger_portal_server_auth_apply`); `docs/mcp/overview.md` (note that the portal aggregate now exposes every mctl-api tool, safety enforced by mctl-api's own checks, not a curated allowlist).
**Rationale:** Both commits landed the same day and change what an MCP client actually sees when connecting through the Backstage/Cloudflare portal aggregate versus a direct connection — a behavioural change any portal-connected user will notice (more tools visible) plus one confirmed-live new tool. `docs/mcp/overview.md` and `docs/mcp/tools-reference.md` currently describe neither, so users following the docs today would under-estimate what's callable through the portal.

### 3. mcp-oauth-flow-fixes: Correct the OAuth dynamic-client-registration / refresh-flow description
**Repo(s):** mctl-api
**Affected commit(s):** 3a88f82, ea2194b, da6192d
**Category:** update-page
**User-visible impact:** 3
**Doc complexity (effort):** 2
**Suggested doc location:** `docs/mcp/connecting.md`, `docs/security/authentication.md`
**Rationale:** Applying the stale-docs-beats-new-gaps tie-break: these three fixes change real, already-shipped behaviour of the OAuth flow used to connect to the MCP portal (client-registration state field, bounded echoed client fields, widened refresh-rotation grace window). If either page currently describes the old grace window or registration mechanics, a user reading it today is reading an inaccurate account of a security-sensitive flow, which outranks writing a new page at similar impact. Spec-writer should read the current content of both pages first to confirm exactly what needs correcting (researcher could only confirm this from titles).

## Dropped
- **mctl-agent alert classification** (5171d37, ffedcbe, "classify ContainerOOMKilled / NodeMemoryHeadroomLow as resource-limit alerts") — real gap, one coherent story, but narrower operator-only audience and equal impact to the OAuth stale-docs item; per the stale-beats-gap tie-break, the stale OAuth docs took the slot instead. Worth reconsidering next cycle if no new stale items compete.
- **19d1d22 GitHub PR webhook producer** — researcher flagged this "moderate confidence," explicitly needing author/maintainer confirmation on whether it's meant to be a publicly documented integration point or stays internal to the lifecycle-reconcile/DevLoop pipeline. Per the "do not invent feature behaviour" rule, this is left for the researcher to get clarification on before it can be scoped as a doc gap.
- **~80 `fix(lifecycle)` / `fix(events)` / `fix(auth)` / `fix(gitops)` internal-hardening clusters** — correctness and credential-handling hardening on already-listed features (lifecycle-ownership, the webhook/outbox pipeline, GitOps cloning). No new user-visible surface beyond what's already covered in gap #1 above; not worth a separate slot.
- **mctl-agents (~127 commits, incl. the ExecutionClaim/shepherd-cadence cluster)** — out of scope for docs.mctl.ai per `context/docs-tree.md`'s own mapping hint, and the active-development portion (from `b64b35b`, 2026-09-18 onward) is explicitly ahead of the deployed 1.46.0 image, i.e. in-flight. Both reasons independently disqualify it.
- **mctl-gitops `cf7c61d`** — auto-generated `chore:` commit, no platform-visible effect.
- **mctl-web, mctl-portal, mctl-openclaw** — empty scan windows, confirmed not an API error; nothing to evaluate, not an infra problem either.
