# Inbox 2026-05-02 (mctl-docs sibling-repo scan)

> Scan range: 2026-04-25 → 2026-05-02 (7 days).
> Mode: GitHub API fallback (Mode B) — local clones absent.
> GITHUB_TOKEN: present (authenticated, 5000 req/h).
> Note: the 2026-05-01 inbox covered 2026-04-24 → 2026-05-01. Today's window
> extends one calendar day (2026-05-01 commits) — that is where new signal lives.

---

## Repo: mctl-api  (prod-version: 4.17.0 — confirmed via mctl-gitops bump `00dc844`)

### Commit f29adbd — feat(mcp): add mctl_trigger_shepherd tool
- **Date:** 2026-05-01
- **Type:** feat
- **User-visible effect:** A sixth admin-only MCP tool, `mctl_trigger_shepherd`, is now
  exposed on the mctl-api MCP server (shipped in mctl-api 4.17.0). It drives existing
  implementer PRs through codex-review fix loops and merges them — the Tier 3 "shepherd"
  step of the mctl-agents pipeline. Parameters: optional `service` (enum of 8 repos, now
  including `mctl-agents`), optional `slug`, optional `dry_run` ("true"/"false"). Returns
  `workflow_name` for status polling. Annotated as destructive (merging is irreversible).
  Cost: ~$1–5 per proposal, ~1–10 min per proposal.
- **Docs:** gap — existing `mcp-agents-tools` proposal covers 5 tools only (016b3c8, f41590e);
  shepherd is absent.
- **Suggested doc location:** `docs/mcp/tools-reference.md` (extend the "mctl-agents pipeline
  controls" section that `mcp-agents-tools` proposes to add)
- **Diff highlight:**
  ```
  // server.go — registration
  srv.AddTool(s.toolTriggerShepherd())   // new, slot 6 of 6

  // comment updated from "Five admin-only tools" to "Six admin-only tools"

  // New tool: mctl_trigger_shepherd
  // POSTs to /api/v1/operations/mctl-agents-shepherd/execute
  // WorkflowNamespace() extended to route mctl-agents-shepherd → argo-workflows ns
  ```

### Commit e1fbe3f — fix(mcp): include mctl-agents in single-service and implementer enums
- **Date:** 2026-05-01
- **Type:** fix
- **User-visible effect:** `mctl-agents` is now a valid value for the `service` parameter
  of both `mctl_trigger_single_service` and `mctl_trigger_implementer`. Admins can now
  self-improve the agents pipeline via MCP (run just the mctl-agents researcher/spec cycle,
  or open implementer PRs against the mctl-agents repo). The `mcp-agents-tools` proposal's
  `proposed-content.md` enumerates 7 service values; it now needs 8.
- **Docs:** stale — `proposals/mcp-agents-tools/proposed-content.md` (unimplemented) lists
  `mctl-web, mctl-openclaw, mctl-docs, mctl-api, mctl-portal, mctl-agent, mctl-gitops` for
  `mctl_trigger_single_service`; `mctl-agents` is missing. Same for `mctl_trigger_implementer`.
- **Suggested doc location:** Update `proposals/mcp-agents-tools/proposed-content.md` to add
  `mctl-agents` to both enums (minor editorial fix, no new proposal needed).

### Commit 878c898 — fix(security): bump jackc/pgx/v5 to v5.9.2 (CVE-2025-54236)
- **Date:** 2026-04-30
- **Type:** fix
- **User-visible effect:** Internal PostgreSQL driver patched for CVE-2025-54236. No user-
  observable behaviour change.
- **Docs:** documented (internal security patch — no docs.mctl.ai action required)

### Commit 5b8ce97 — fix(security): cap OAuth handler form bodies with MaxBytesReader
- **Date:** 2026-05-01
- **Type:** fix
- **User-visible effect:** Internal hardening — OAuth token/revoke endpoints now cap inbound
  bodies at 16 KiB, preventing memory-exhaustion abuse. Not observable by normal callers.
- **Docs:** documented (internal security hardening — no docs.mctl.ai action required)

---

## Repo: mctl-web  (prod-version: unverified)

Previously scanned commits (`87fdc89`, `802d418`) already recorded in 2026-04-30 inbox.
No new commits in 2026-05-01. No doc gaps.

---

## Repo: mctl-portal  (prod-version: unverified)

Previously scanned commits already recorded in 2026-05-01 inbox. No new commits in
2026-05-01. No doc gaps.

---

## Repo: mctl-agent  (prod-version: unverified)

### Commit 31b8e59 — chore(deps): bump modernc.org/sqlite to v1.37.0 (CVE-2025-6965)
- **Date:** 2026-05-01
- **Type:** chore
- **User-visible effect:** Internal SQLite driver dependency update for CVE-2025-6965. No
  user-observable behavior change.
- **Docs:** documented (chore/security patch — no docs.mctl.ai action required)

`f4e8a38` (per-tenant Telegram routing) already recorded in 2026-04-30 inbox;
`proposals/mctl-agent-telegram-routing/` exists.

---

## Repo: mctl-agents  (prod-version: unverified)

All commits are internal agent framework changes (shepherd module, implementer fixes,
orchestrator hardening). Per `context/docs-tree.md`: mctl-agents is NOT documented on
docs.mctl.ai. All dropped.

---

## Repo: mctl-gitops  (prod-version: unverified)

Notable 2026-05-01 commits:
- `00dc844` — `chore: bump mctl-api to 4.17.0` — confirms shepherd tool (f29adbd) is in
  production.
- `6c0ba41` — `feat(workflows): un-suspend mctl-agents-shepherd CronWorkflow` — shepherd
  is now live and running automatically; relevant context for `mcp-shepherd-tool` proposal.
- `ff056b8` — `feat(workflows): add mctl-agents-shepherd CWFT + CronWorkflow (suspended)` —
  the Argo ClusterWorkflowTemplate that backs `mctl_trigger_shepherd` landed here.
- Remaining ~50 commits: `chore(agents):` pipeline runs, security bumps, infra fixes — all
  internal.

---

## Repo: mctl-openclaw  (prod-version: unverified)

New 2026-05-01 commits: none (last meaningful commits were 2026-04-28, already covered in
2026-04-30 inbox). All remaining CI/test/fix commits are internal. No new doc gaps.

Previously identified gaps: `openclaw-outbound-sanitization`, `openclaw-docker-skip-onboarding`,
`openclaw-gateway-handshake-timeout` proposals already exist.

---

## Summary
- Total commits scanned: ~215 across 7 repos
- **New since 2026-05-01 inbox:** 2 actionable (f29adbd gap, e1fbe3f stale), 4 internal drops
- gap: 1 new (`mcp-shepherd-tool`)
- stale: 1 new (enum update to unimplemented `mcp-agents-tools` proposed-content)
- documented: 0 new
- in-flight (do NOT propose): 0

---

## Top-3 (for spec-writer)

### 1. mcp-shepherd-tool: Document the Tier 3 mctl_trigger_shepherd MCP tool
**Repo(s):** mctl-api, mctl-gitops
**Affected commit(s):** f29adbd, 6c0ba41 (gitops un-suspend confirms it is in production)
**Category:** update-page
**User-visible impact:** 5
**Doc complexity (effort):** 2
**Suggested doc location:** `docs/mcp/tools-reference.md` (add `mctl_trigger_shepherd`
entry to the "mctl-agents pipeline controls" section proposed by `mcp-agents-tools`)
**Rationale:** A sixth admin-only MCP tool, `mctl_trigger_shepherd`, shipped in mctl-api
4.17.0 on 2026-05-01 and the CronWorkflow is already un-suspended in production. The
existing `mcp-agents-tools` proposal (unimplemented) covers only five tools; shepherd
is absent. Any admin who discovers it via `tools/list` sees no documentation. The tool
is annotated destructive (it merges PRs) — documenting the `dry_run` flag and cost
profile is especially important. Effort is low because the tool description in the source
is detailed and self-documenting.

### 2. mcp-agents-tools stale-enum: Add mctl-agents to service enum in mcp-agents-tools proposal
**Repo(s):** mctl-api
**Affected commit(s):** e1fbe3f
**Category:** update-page (editorial fix to unimplemented proposal)
**User-visible impact:** 2
**Doc complexity (effort):** 1
**Suggested doc location:** `proposals/mcp-agents-tools/proposed-content.md` — add
`mctl-agents` to the `service` enum rows for `mctl_trigger_single_service` and
`mctl_trigger_implementer`
**Rationale:** The `mcp-agents-tools` proposed-content lists 7 service values for the
`service` parameter; `mctl-agents` was added as the 8th in `e1fbe3f`. Since the proposal
is not yet implemented the fix belongs in the proposed-content before it is applied, not
as a separate follow-up PR. Minimal effort — a two-cell table edit. Not a standalone
proposal; this is a note for the implementer or spec-writer to update the existing file.

### 3. (no third distinct gap this week)
No third genuinely new user-visible gap exists in the 2026-04-25 → 2026-05-02 window
beyond those already captured in prior inbox files and existing proposals. The queue of
unimplemented proposals (`mcp-agents-tools`, `openclaw-outbound-sanitization`,
`mctl-agent-telegram-routing`, and others) remains the primary backlog.

## Dropped
- **mctl-api pgx CVE patch** (`878c898`): internal dep upgrade, no user behaviour change.
- **mctl-api OAuth form body cap** (`5b8ce97`): internal security hardening, not user-visible.
- **mctl-agent sqlite CVE patch** (`31b8e59`): chore, no user behaviour change.
- **mctl-gitops infra chores** (shepherd CronWorkflow, ArgoCD bumps, ESO upgrade): all
  deployment/infra, no standalone docs.mctl.ai gap beyond what's noted in the shepherd
  proposal.
- **mctl-openclaw all 2026-05-01 commits**: none (no new commits that day).
- **mctl-agents all commits**: excluded per `context/docs-tree.md`.
- **mctl-web, mctl-portal**: no new commits since 2026-04-30.
