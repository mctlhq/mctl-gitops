# Inbox 2026-04-30 (mctl-docs sibling-repo scan)

> Scan range: 2026-04-23 → 2026-04-30 (7 days).
> Mode: GitHub API fallback (Mode B) — local clones absent.
> GITHUB_TOKEN: present (authenticated, 5000 req/h).

---

## Repo: mctl-api  (prod-version: unverified)

### Commit 016b3c8 — feat(mcp): mctl-agents trigger tools (run / mentor-only / single-service / list)
- **Date:** 2026-04-27
- **Type:** feat
- **User-visible effect:** Four new admin-only MCP tools are exposed on the mctl-api MCP server: `mctl_trigger_agents_run` (full pipeline), `mctl_trigger_mentor_only`, `mctl_trigger_single_service` (filter to one service-agent), and `mctl_list_recent_agent_runs`. A matching REST endpoint `GET /api/v1/agent-runs` also lands. Any admin user with Claude/MCP access can now drive the mctl-agents pipeline on demand without touching ArgoCD.
- **Docs:** gap
- **Suggested doc location:** `docs/mcp/tools-reference.md` (new section "mctl-agents pipeline controls")

### Commit f41590e — feat(mcp): add mctl_trigger_implementer admin tool
- **Date:** 2026-04-29
- **Type:** feat
- **User-visible effect:** Fifth mctl-agents MCP tool: `mctl_trigger_implementer` triggers Tier 2 implementer agents that scan accepted proposals and open pull requests in the relevant sibling repo. Optional `service`, `slug`, and `force` parameters narrow scope. Admin-only; returns `workflow_name` for status polling.
- **Docs:** gap
- **Suggested doc location:** `docs/mcp/tools-reference.md` (same new section, alongside the other four tools above)

### Commit e4f104d — fix(openclaw): route identity workflows to argo-workflows + skill quota filter
- **Date:** 2026-04-23
- **Type:** fix
- **User-visible effect:** OpenClaw identity operations (save/delete identity, skill quota enforcement) now route to the dedicated `argo-workflows` namespace instead of the tenant namespace. Users who call identity MCP tools will see the workflow appear in `argo-workflows` rather than their tenant namespace.
- **Docs:** stale (page: `docs/platform/openclaw.md` — workflow namespace routing not described)
- **Suggested doc location:** `docs/platform/openclaw.md`

### Commit 92fbf9e — fix(openclaw): block openclaw ops from generic /execute path
- **Date:** 2026-04-23
- **Type:** fix
- **User-visible effect:** OpenClaw-specific operations (skill-save, skill-delete, identity-save, identity-delete) are now rejected on the generic `/execute` REST endpoint; callers must use the dedicated OpenClaw API paths. Integrations that went through `/execute` will receive a 400 error.
- **Docs:** stale (page: `docs/platform/openclaw.md` — endpoint routing not documented)
- **Diff highlight:** `registry.go` now marks these four operations with `OpenClawOnly: true`; `handlers_write.go` returns 400 if the flag is set and the request arrived via the generic path.

### Commit edba139 — fix(openclaw): restrict identity listing to the fixed allowlist
- **Date:** 2026-04-23
- **Type:** fix
- **User-visible effect:** Identity listing is now restricted to a fixed allowlist; callers outside the allowlist receive an error. Minor behavior change for platform operators using identity listing API.
- **Docs:** stale (page: `docs/platform/openclaw.md`)

---

## Repo: mctl-web  (prod-version: unverified)

No commits in range 2026-04-23 → 2026-04-30.

---

## Repo: mctl-portal  (prod-version: unverified)

No commits in range 2026-04-23 → 2026-04-30.

---

## Repo: mctl-agent  (prod-version: unverified)

### Commit f4e8a38 — feat(notify): per-tenant Telegram routing
- **Date:** 2026-04-27
- **Type:** feat
- **User-visible effect:** mctl-agent now routes alert notifications to per-tenant Telegram chat IDs. Platform operators can set `TELEGRAM_TENANT_CHAT_IDS="admins=123,labs=456,ovk=789"` (or per-tenant env vars `TELEGRAM_CHAT_ID_ADMINS` etc.); if set, each tenant's alerts go to its own Telegram chat. Without the new env vars the agent falls back to the existing global `TELEGRAM_CHAT_ID`.
- **Docs:** gap
- **Suggested doc location:** `docs/platform/components.md` (mctl-agent subsection — new "Telegram routing" paragraph)
- **Diff highlight:** `internal/config/config.go` adds `TelegramTenantChatIDs map[string]string`, parsed from `TELEGRAM_TENANT_CHAT_IDS` CSV env var or individual `TELEGRAM_CHAT_ID_<TENANT>` variables.

---

## Repo: mctl-agents  (prod-version: unverified)

All commits in range are internal (agent framework scaffolding, i18n, orchestrator fixes, self-improvement). Per `context/docs-tree.md`: "mctl-agents (this repo!) → NOT documented in docs.mctl.ai (internal tool)." All dropped.

---

## Repo: mctl-gitops  (prod-version: unverified)

All user-visible changes in this repo are deployment bumps or infra wiring that echo the mctl-api, mctl-agent, and mctl-openclaw changes already recorded above. No independent doc gap.

Notable deploy commits (for version tracking):
- `dd3906e` 2026-04-29 — `deploy: admins/mctl-docs → 0.1.16`
- `1c59903` 2026-04-27 — `chore: bump mctl-agent to 1.6.0`
- `9595716` 2026-04-27 — `chore: bump mctl-api to 4.15.0`
- `2ec9463` 2026-04-29 — `chore: bump mctl-api to 4.16.0`

---

## Repo: mctl-openclaw  (prod-version: unverified)

### Commit c5c08c0 + 1e9faa2 + 98f5fd1 — inter-session prompt guards (security feature)
- **Date:** 2026-04-28
- **Type:** feat / docs (in openclaw repo)
- **User-visible effect:** OpenClaw now marks injected inter-session context (e.g. system reminders) with a provenance tag so the gateway can strip them from outbound channel delivery. The tags `<system-reminder>` and `<previous_response>` are explicitly stripped alongside `<tool_call>` / `<function_calls>` at the channel boundary. Users on all three tenants (admins/labs/ovk) no longer see internal scaffolding leaking into Telegram/Slack/Discord replies.
- **Docs:** gap (the openclaw repo's own `docs/` is updated, but `docs.mctl.ai/platform/openclaw.md` has no mention of outbound stripping or inter-session safety)
- **Suggested doc location:** `docs/platform/openclaw.md` (new "Outbound content sanitization" subsection) and possibly `docs/security/authentication.md`
- **Diff highlight:** New `src/sessions/input-provenance.ts` tracks prompt origin; `src/auto-reply/reply/prompt-prelude.ts` injects provenance markers; `docs/reference/transcript-hygiene.md` (openclaw-repo) lists stripped tags.

### Commit 490e6d6 — feat(docker): add OPENCLAW_SKIP_ONBOARDING env
- **Date:** 2026-04-28
- **Type:** feat
- **User-visible effect:** Operator-only Docker env var `OPENCLAW_SKIP_ONBOARDING=true` skips the interactive onboarding wizard during container startup. Relevant for automated deployments.
- **Docs:** gap
- **Suggested doc location:** `docs/platform/openclaw.md` (deployment/env vars section) — **low priority**, operator-only.

---

## Summary
- Total commits scanned: ~200 (mctl-api: 9, mctl-agent: 2, mctl-agents: 24 dropped, mctl-gitops: ~80 mostly chore, mctl-openclaw: ~80 mostly CI/test/sync)
- gap: 3 (mcp-tools-reference: 5 new MCP tools; mctl-agent Telegram routing; openclaw outbound sanitization)
- stale: 1 (openclaw.md — routing/security behavior)
- documented: 0
- in-flight (do NOT propose): 0

---

## Top-3 (for spec-writer)

### 1. mcp-agents-tools: Document 5 new mctl-agents MCP tools
**Repo(s):** mctl-api
**Affected commit(s):** 016b3c8, f41590e
**Category:** update-page
**User-visible impact:** 5
**Doc complexity (effort):** 3
**Suggested doc location:** `docs/mcp/tools-reference.md` (add new section "mctl-agents pipeline controls")
**Rationale:** Five new admin-only MCP tools land in mctl-api 4.15.0–4.16.0. Any admin using Claude with MCP will discover them via `tools/list` but find zero documentation on what they do, what the parameters mean, or what to poll for status. Impact is maximum for the admin audience — these are the primary on-demand controls for the platform's autonomous improvement pipeline. Effort is moderate: the code is well-commented and the tool descriptions embed good prose; the doc writer mainly needs to surface them in a table + examples.

### 2. openclaw-outbound-sanitization: Document OpenClaw outbound content stripping
**Repo(s):** mctl-openclaw
**Affected commit(s):** c5c08c0, 1e9faa2, 98f5fd1
**Category:** update-page
**User-visible impact:** 4
**Doc complexity (effort):** 2
**Suggested doc location:** `docs/platform/openclaw.md` (new "Outbound content sanitization" subsection); cross-reference from `docs/security/authentication.md`
**Rationale:** OpenClaw now actively strips `<system-reminder>`, `<previous_response>`, `<tool_call>`, and `<function_calls>` tags before channel delivery — a meaningful security property for multi-tenant deployments. Operators running labs/ovk tenants need to know this guarantee exists (and its limits). The fix also changes observable behaviour: inter-session prompt leakage that some users may have seen is now blocked. Effort is low because the upstream openclaw changelog already documents the list of stripped tags.

### 3. mctl-agent-telegram-routing: Document per-tenant Telegram routing in mctl-agent
**Repo(s):** mctl-agent
**Affected commit(s):** f4e8a38
**Category:** update-page
**User-visible impact:** 3
**Doc complexity (effort):** 2
**Suggested doc location:** `docs/platform/components.md` (mctl-agent subsection — add "Alert routing" paragraph)
**Rationale:** Platform operators managing multiple tenants now have a way to route mctl-agent alerts to separate Telegram chats per tenant, but the env vars (`TELEGRAM_TENANT_CHAT_IDS`, `TELEGRAM_CHAT_ID_<TENANT>`) are not documented anywhere on docs.mctl.ai. Without documentation, operators will misconfigure (or miss) this capability. Effort is low — one short paragraph plus a config table.

## Dropped
- **mctl-openclaw OPENCLAW_SKIP_ONBOARDING** (490e6d6): operator-only Docker config, very low user-visible impact; can be picked up in a future operator-guide proposal if one is written.
- **mctl-api openclaw routing fixes** (e4f104d, 92fbf9e, edba139): user-visible behavior changes but they are security hardening internals (namespace routing, allowlist enforcement) that do not require documentation action beyond a note in `docs/platform/openclaw.md` — subsumed into slug #2 (openclaw-outbound-sanitization) as context.
- **mctl-gitops infra chores**: all deployment/config changes, no standalone doc gap.
- **mctl-agents commits**: explicitly excluded per `context/docs-tree.md` ("NOT documented in docs.mctl.ai").
- **mctl-web, mctl-portal**: no commits this week.
