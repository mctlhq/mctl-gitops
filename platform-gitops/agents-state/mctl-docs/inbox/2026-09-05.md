# Inbox 2026-09-05 (mctl-docs sibling-repo scan)

Scan window: 2026-08-29T00:00:00Z .. 2026-09-05 (7 days). Mode B (GitHub API,
authenticated, `$GITHUB_TOKEN`) used for every repo — no local clones exist in
this container. Cross-checked against `context/docs-tree.md` and current
production versions via `mcp__mctl__*` (mctl_list_services,
mctl_list_operations, mctl_list_platform_skills) plus the `chore: bump
<team>/<service> to X.Y.Z` commits in mctl-gitops, which give a reliable
"is this SHA actually deployed" signal even for repos not tracked by
mctl_list_services (mctl-api, mctl-portal, mctl-agent are core platform
infra, not tenant workloads, so they don't appear in that list).

## Repo: mctl-api  (prod: 4.37.0 — confirmed via mctl-gitops `bump admins/mctl-api to 4.37.0` (3441a91) and by mctl_list_operations already showing mctl-agents-approve/mctl-agents-reconcile live)

### Commit 1c6a746 — feat(operations): add mctl-agents-approve operation
- **Date:** 2026-08-29
- **Type:** feat
- **User-visible effect:** Admins can now approve an mctl-agents proposal (flip `.status.yaml` proposed→accepted) as a first-class platform operation instead of a manual gitops edit. Confirmed live in production — it appears in `mctl_list_operations` today.
- **Docs:** gap — no page in docs.mctl.ai documents the "platform operations" catalog (`mctl_list_operations`/`mctl_get_operation`) at all; only individual MCP tools are covered in `docs/mcp/tools-reference.md`.
- **Suggested doc location:** docs/mcp/tools-reference.md (new subsection "Platform operations vs. MCP tools") or a new docs/reference/operations.md.

### Commit 20db091 — feat(operations): add the mctl-agents-reconcile operation
- **Date:** 2026-09-01
- **Type:** feat
- **User-visible effect:** New operation reconciles GitHub PR state onto proposal `.status.yaml` (merged/closed/needs-triage) without invoking a model. Also confirmed live via `mctl_list_operations`.
- **Docs:** gap — same missing "operations catalog" page as above.
- **Suggested doc location:** docs/mcp/tools-reference.md or docs/reference/operations.md.

### Commit 3dee7e9 — feat(agents): expose durable DevLoop approve endpoint (issue-228)
- **Date:** 2026-09-02
- **Type:** feat
- **User-visible effect:** Adds a durable Temporal "approve" signal path for DevLoopWorkflow runs, distinct from the gitops-file `mctl-agents-approve` operation above. The tool descriptions I have access to (`mctl_approve_dev_loop`) explicitly warn callers not to confuse the two paths — a strong sign this distinction needs an explainer somewhere a human would read it, not just in a tool docstring.
- **Docs:** gap — dev-loop approval semantics (durable-signal vs. direct `.status.yaml` mutation) are undocumented on docs.mctl.ai.
- **Suggested doc location:** docs/guides/gitops-workflows.md (new "Approving agent proposals" section) or docs/mcp/tools-reference.md.

### Commit d6aca27 — feat(dev-loop): describe endpoint for workflow liveness
- **Date:** 2026-08-29
- **Type:** feat
- **User-visible effect:** New REST endpoint reports whether a DevLoop workflow execution is still alive. Minor, admin-facing.
- **Docs:** gap (minor) — docs/api/index.md does not mention dev-loop endpoints.
- **Suggested doc location:** docs/api/index.md.

### Commit 58aec9d — fix(deps): bump golang.org/x/crypto to v0.55.0 for CVE-2026-56854
- **Date:** 2026-09-01
- **Type:** fix
- **User-visible effect:** Security dependency patch; no behavior change for API/MCP consumers.
- **Docs:** documented (no doc change needed; nothing in docs.mctl.ai references crypto library versions).

### Commits 1bd6674 / c819c38 — fix(agents): repos-sync-proxy stops forwarding credentials it shouldn't
- **Date:** 2026-08-29 / 2026-08-29
- **Type:** fix
- **User-visible effect:** `mctl_sync_repos` now derives the account strictly from the authenticated caller and no longer leaks a credential to the sync proxy it didn't need. Restores documented behavior; not a new capability.
- **Docs:** documented (page: docs/guides/services.md — repo access section already describes sync_repos at the level users need; this is a correctness fix underneath it).

### In-flight cluster (NOT yet in production — no `bump admins/mctl-api` commit past 4.37.0 in this window, and the new `mctl_trigger_reconcile`/`mctl_trigger_approve` MCP tools referenced below are not present in my current toolset, corroborating that they haven't shipped)
- 04fec23 — feat(mcp): add mctl_trigger_reconcile and mctl_trigger_approve tools
- 9ea3ee2 — feat(agents): issue-238 (two verified gaps from review) — unclear scope, needs author clarification
- 33685a0 — fix(agents): the dev-loop approver is a fact about the caller, not a body field
- 069b53a — fix(api): the approve operation's approver is the caller, not a body field
- 0f974ae — fix(api): make IsService proof of authentication, and audit the denial
- 5c5156b — fix(api): filter undeclared parameters after authentication, not before
- e3d57c7 — fix(api): stop forwarding undeclared parameters to Argo
- 00fdf8d — fix(release): open release PRs with an App token, not GITHUB_TOKEN
- **Docs:** in-flight, do NOT document yet. Once released, 04fec23 (new MCP tool wrappers around the two operations above) closes part of the operations-catalog gap noted above — worth re-checking next week.

## Repo: mctl-web  (prod: 7.5.3 — confirmed directly via mctl_list_services, imageTag matches latest `chore(main): release 7.5.3`)

### Commits f4383bb, 3cc400a, e5b1131, 9cba9a3, 4f5e5f9, ba30f8e, b85ec33, 0a34992, 3f891bf, 8012b69 — feat(turnstile): enforce Turnstile + identity-gate on check-team forms (#66)
- **Date:** 2026-09-01
- **Type:** feat
- **User-visible effect:** mctl.ai's contact/check-team forms now require a Turnstile CAPTCHA pass and reject requests that don't carry an identity-gated cookie; several follow-up fixes closed CSP, rate-limit and availability-check bugs in the same feature.
- **Docs:** documented — not applicable. This is an anti-abuse measure on the marketing site (mctl.ai), not the platform product documented at docs.mctl.ai; no onboarding flow the docs cover is affected.

### Commits 3f20adb, 90c726e, 5e0cf58, 3761b3b — fix(worker): key the mctl.ai rate limiter by method/path/initiator
- **Date:** 2026-09-01
- **Type:** fix
- **User-visible effect:** Rate-limit bucketing bug fixes on the mctl.ai Cloudflare Worker; no platform-docs-relevant behavior.
- **Docs:** documented — not applicable (mctl.ai marketing site internals, out of docs.mctl.ai's scope).

### Note
- 1e68832 (ci: switch to release-please) and remaining commits this week are CI/build internals — dropped per filter, no doc impact.

## Repo: mctl-portal  (prod: 4.15.0 — confirmed via mctl-gitops `bump admins/mctl-portal to 4.15.0` (61cf7eb); no later bump found in this window)

### In-flight cluster (after the 4.15.0 release commit, not yet bumped in mctl-gitops)
- 10e2553 — fix(team-policy): type the catalog anyOf array so tsc accepts the read-gated arm
- 88a6ba8 — fix(agents): issue-102 — team-policy: any member could delete or reference a team's resources it shouldn't have been able to
- **User-visible effect (once shipped):** Tightens team-policy authorization so only appropriately-privileged members can delete/reference certain team resources in the Backstage portal — a real behavior change to something docs/security/authorization.md describes at a high level.
- **Docs:** in-flight, do NOT document yet. Re-check after the next mctl-portal release; if shipped, docs/security/authorization.md should be checked for accuracy (page: docs/security/authorization.md, potentially stale once this ships).

### Aggregate note — shipped security hardening, no new capability
Commits e7d3864 (CVE-2024-21534 jsonpath-plus RCE fix), b58419d, 9da39e3, c1037c1, a2cf0f0, 93f5b6d, 4727aa9 (OIDC returnTo allowlist hardening), 66f3457/e07861a (Docker base-image fixes) all shipped in 4.14.1–4.15.0. These are correctness/security fixes to already-documented behavior (team policy, OIDC login), not new capabilities.
- **Docs:** documented (docs/security/authentication.md and docs/security/authorization.md remain accurate at the level of detail they currently cover).

## Repo: mctl-agent  (prod: 2.0.1 — confirmed via mctl-gitops `bump admins/mctl-agent to 2.0.1` (db6671e))

### Aggregate note
All 23 non-merge commits this week (`fix(pipeline)`, `fix(ticket)`, `fix(remote)`, `fix(monitor)`) are internal correctness/security fixes to the self-healing agent's own state machine (transition guards, DNS-rebinding TOCTOU close, context propagation). None change the user-facing behavior described in docs/platform/components.md's mctl-agent block or docs/reference/troubleshooting.md.
- **Docs:** documented (no page needs updating).

## Repo: mctl-agents  (this is the mctl-agents R&D repo — per docs-tree, NOT documented on docs.mctl.ai; internal tool)

### Aggregate note
193 non-merge commits this week — entirely internal to the mctl-agents pipeline itself (investigator hardening against symlink/TOCTOU attacks, Temporal worker drain/shutdown correctness, dev-loop shepherd/merge-watch logic, resolver/catalog validation). Per `context/docs-tree.md`, this repo is explicitly out of scope for docs.mctl.ai.
- **Docs:** documented — not applicable (internal tool, by design).

## Repo: mctl-gitops  (source of truth; prod n/a — this repo IS the deploy target)

**Note: pagination cap hit.** This repo produced far more than the 5-page (500-commit) cap in the 7-day window; page 5 still showed `Link: rel="next"`. The commits below are drawn from the first ~450 (5 pages); older activity in the window may be truncated. Flagging per skill instructions: `note: mctl-gitops hit 5-page cap, digest may be incomplete`.

The overwhelming majority of the ~450+ commits fetched are either (a) automated `chore: bump <team>/<service> to X.Y.Z` image-tag bumps from other repos' releases (no independent signal — already captured under each source repo above), or (b) the mctl-agents pipeline's own operational commits against itself (`chore(agents): investigate/implement/approve/reconcile/shepherd ...` for issues in mctl-telegram, mctl-api, mctl-portal, etc.) and internal ArgoCD-workflow-template security hardening (shell-injection/yq-injection closures, SSH host-key pinning, secret redaction). None of this is user-facing product behavior a docs.mctl.ai reader would look up.

### Commits fc35b55, cae7549, 6d0c3d0 — feat(platform-skills): publish review-watch
- **Date:** 2026-09-01
- **Type:** feat
- **User-visible effect:** A new platform-wide skill "review-watch" (admin-only visibility) was published to the platform skills catalog; confirmed live via `mctl_list_platform_skills` today. This is the third `platform-skills: publish` commit in the window — `archify-diagrams` (public) was also added this week per the catalog's `lastModified` timestamps.
- **Docs:** gap — the entire "platform skills" concept (`mctl_list_platform_skills`, `mctl_enable_tenant_skill`, `mctl_read_platform_skill`, visibility tiers public/tenant/admin) has no page anywhere in docs.mctl.ai's current structure. This is broader than one commit; flagging the structural gap here for the analyst to size.
- **Suggested doc location:** new docs/platform/skills.md, or a subsection of docs/mcp/tools-reference.md.

### Commit a34521b — feat(argocd): per-push freshness check and stalled-polling alert (#970)
- **Date:** 2026-09-02
- **Type:** feat
- **User-visible effect:** ArgoCD now syncs faster after a push (webhook + scoped refresh) and alerts if git polling stalls — improves deploy latency admins may notice, but it's an operational improvement, not a new documented capability or contract change.
- **Docs:** documented (docs/guides/gitops-workflows.md already describes "push to main → auto-deploy" at the level users need; the latency improvement doesn't change that contract).

## Repo: mctl-openclaw

- No commits returned by the GitHub API for the 2026-08-29..2026-09-05 window (empty result, confirmed HTTP 200 with `[]` body — not a rate-limit or access failure).

## Summary
- Total commits scanned: 747 (mctl-api 46, mctl-web 34, mctl-portal 19, mctl-agent 23, mctl-agents 193, mctl-gitops 432 fetched — capped at 5 pages, more exist beyond the cap, mctl-openclaw 0)
- gap: 5 (mctl-agents-approve operation, mctl-agents-reconcile operation, dev-loop durable-approve endpoint, dev-loop describe endpoint, platform-skills catalog undocumented)
- stale: 0
- documented: 6 (crypto CVE bump, repos-sync-proxy credential fix, mctl-web turnstile, mctl-web rate-limiter fixes, mctl-portal security-hardening aggregate, mctl-agent aggregate, ArgoCD freshness check — counted as one line item where genuinely one topic)
- in-flight (do NOT propose): 2 clusters, 10 underlying commits (mctl-api approve/reconcile follow-up hardening + new MCP tool wrappers not yet released; mctl-portal team-policy delete/reference fix not yet released)
- no signal: mctl-openclaw (no commits this week, confirmed via API, not a fetch failure)
- note: mctl-gitops commit history exceeded the 5-page/500-commit pagination cap; digest for that repo may be incomplete for the tail of the window.

## Top-3 (for spec-writer)

### 1. platform-operations-approval-flow: Document the platform operations catalog and the two approval paths for agent proposals
**Repo(s):** mctl-api
**Affected commit(s):** 1c6a746, 20db091, 3dee7e9
**Category:** new-page
**User-visible impact:** 5
**Doc complexity (effort):** 3
**Suggested doc location:** docs/reference/operations.md (new page for the `mctl_list_operations`/`mctl_get_operation` catalog, covering `mctl-agents-approve` and `mctl-agents-reconcile`), with a linked "Approving agent proposals" subsection in docs/guides/gitops-workflows.md that explains the durable DevLoop `mctl_approve_dev_loop` Temporal-signal path vs. the gitops-file `mctl-agents-approve` operation, and a cross-link from docs/mcp/tools-reference.md.
**Rationale:** All three commits are one story — admins now have a first-class "platform operations" surface for approving/reconciling agent proposals, but docs.mctl.ai has no page describing that surface at all. The `mctl_approve_dev_loop` tool docstring explicitly warns callers not to confuse the durable-signal path with the direct `.status.yaml` mutation, which is a strong, author-flagged signal that this distinction needs a human-readable explainer, not just an in-code warning — this is the kind of gap that causes real operator mistakes if left undocumented. Both underlying operations are confirmed live in production (4.37.0, verified via `mctl_list_operations`).

### 2. platform-skills-catalog: Document the platform skills catalog and visibility tiers
**Repo(s):** mctl-gitops
**Affected commit(s):** fc35b55, cae7549, 6d0c3d0
**Category:** new-page
**User-visible impact:** 4
**Doc complexity (effort):** 4
**Suggested doc location:** docs/platform/skills.md (new page covering `mctl_list_platform_skills`, `mctl_enable_tenant_skill`, `mctl_read_platform_skill`, and the public/tenant/admin visibility tiers), with a cross-link from docs/mcp/tools-reference.md.
**Rationale:** This is a structurally distinct, entirely undocumented concept — not a bug fix or minor addition. Two skills (`review-watch`, admin-only; `archify-diagrams`, public) are now live in the catalog per `mctl_list_platform_skills`, confirmed today, and users/tenants have no way to discover what a "platform skill," a visibility tier, or `mctl_enable_tenant_skill` even means from current docs. This is a whole new capability surface, not an isolated commit.

### 3. dev-loop-describe-endpoint: Document the DevLoop workflow-liveness `describe` endpoint
**Repo(s):** mctl-api
**Affected commit(s):** d6aca27
**Category:** update-page
**User-visible impact:** 2
**Doc complexity (effort):** 1
**Suggested doc location:** docs/api/index.md
**Rationale:** Minor but genuinely missing — docs/api/index.md doesn't mention dev-loop endpoints at all, and this new admin-facing liveness check is a small, low-effort addition to close that gap while doc-writers are already touching the dev-loop area for item #1 above. Lowest priority of the three because impact is narrow (admin-only diagnostics, not a new workflow or safety concern).

## Dropped
- **Commits 1c6a746, 20db091, 3dee7e9 as separate slots** — merged into Top-3 #1. Per the "whole stories beat isolated commits" rule, these three form one coherent admin-approval-flow story (operations catalog + the approve/reconcile operations + the durable-signal distinction), not three independent doc gaps; splitting them into separate slots would have wasted 2 of the 3 available slots on one theme and pushed out the platform-skills gap.
- **mctl-api in-flight cluster** (04fec23 `mctl_trigger_reconcile`/`mctl_trigger_approve` MCP tool wrappers, plus the associated caller/param-filtering fixes 9ea3ee2, 33685a0, 069b53a, 0f974ae, 5c5156b, e3d57c7, 00fdf8d) — explicitly in-flight, not shipped past mctl-api 4.37.0. Per the "do not document in-flight code" rule, skipped entirely. Worth re-checking next week: once released, 04fec23 will further consolidate the operations-catalog story from Top-3 #1.
- **mctl-portal team-policy delete/reference fix** (10e2553, 88a6ba8) — in-flight, not shipped past mctl-portal 4.15.0. Would be a legitimate "stale docs" candidate for docs/security/authorization.md once it ships (a real authorization behavior change), but cannot be proposed yet per the in-flight rule. Re-check after the next mctl-portal release/bump.
- **mctl-web Turnstile/identity-gate feature and rate-limiter fixes** — correctly out of scope; these are mctl.ai marketing-site internals, not docs.mctl.ai platform product surface.
- **mctl-portal shipped security-hardening aggregate** (jsonpath-plus CVE fix, OIDC returnTo allowlist, Docker base-image fixes) and **mctl-api crypto CVE bump / repos-sync-proxy credential fix** — already-documented behavior receiving correctness/security fixes underneath, no doc text needs to change; not a gap.
- **mctl-agent aggregate** (23 internal state-machine correctness fixes) and **mctl-agents repo** (193 internal commits) — both internal-only per `context/docs-tree.md`; no reader-facing surface changed.
- **ArgoCD per-push freshness check** (a34521b) — operational latency improvement; the documented contract ("push to main → auto-deploy") is unchanged, so no doc update needed.
- **mctl-openclaw (no commits this week)** — infra/no-signal, not a doc gap; correctly not spending a slot on it.
