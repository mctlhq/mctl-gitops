status: accepted
updated_at: '2026-05-07T07:10:00Z'
updated_by: mashkovd
notes: |
  Operator-seeded proposal (skipping mentor digest). Second half of the
  Phase 4 split. Phase 4a (mctl-agent 1.11.0, PR #16) shipped the
  prom/client_golang dependency, /metrics endpoint, single counter,
  ServiceMonitor (canonical copy in mctl-gitops at bac7212).

  4b adds the remaining five metric handles, wires them at every
  resolve / guard / AM-call site, observes AM request duration, adds
  the OpenTicketBreakdown helper + gauge update in poll cycle, and
  cleans up the misplaced ServiceMonitor file in the mctl-agent repo
  (canonical lives in mctl-gitops).

  Sized for the $3 implementer budget by referencing the original
  Phase 4 attempts (which exhausted budget on broader scope) and
  Phase 2 (PR #13, 659+/27- across 10 files) which fit. tasks.md is
  intentionally explicit about insertion points to minimise
  implementer exploration.
