# Mentor digest — 2026-W19 (final pass, 2026-05-07)

_This pass replaces every earlier W19 draft at this path. Output is
English only; any non-English fragments referenced from upstream
proposals or prior drafts have been paraphrased into English on the
way in._

**Active platform services (7):** `mctl-web`, `mctl-openclaw`,
`mctl-docs`, `mctl-api`, `mctl-portal`, `mctl-agent`, `mctl-gitops`.

> **Verification note.** `mcp__mctl__*` tools are still unavailable in
> this session (`ToolSearch query="mctl"` returns no matches). Cross-
> checks against current service versions, open incidents, and tenant
> resource limits are therefore textual only — taken from each
> service's `context/current-version.md`, `CLAUDE.md`, and prior W19
> drafts treated as already-decided context. Restoring `mcp__mctl__*`
> is itself a Platform-risks item, now blocking five consecutive
> Top-5 picks.

---

## Scope of this pass

The Wednesday-W19 refresh and the two W18 drafts already named
2026-W19's anchor work — openclaw 2026.5.4, Backstage v1.50.4
twelve-bundle, mcp-go v0.52.0, Go 1.26.2 cross-repo, ArgoCD v3.4.0
GA, plus a Thursday add-on Top-5 covering the Loki Ruler path-traversal
patch, the ArgoCD client-token rotation, the MCP-hardening triple, the
Helm-binary triple-GHSA, and the cert-manager DNS-DoS patch. Those
items are **already triaged** and remain in flight.

Per the mentor charter, this pass considers only proposals that did not
appear in any earlier digest. Walking
`/app/agents/<service>/proposals/` for all seven services returned
**zero new files** since the Thursday refresh. `inbox/` directories
are also empty for every service. There is therefore no Top-5 to
promote on this pass; the Wednesday + Thursday W19 Top-5 stands as
the W19 record of all proposals ready for review.

## Top 5 proposals ready for review (this pass)

| # | Service | Proposal | Impact | Effort | Conflicts | Fit |
|---|---------|----------|--------|--------|-----------|-----|
| — | —       | _no fresh proposals since the Thursday-refresh pass_ | — | — | — | — |

For the Top-5 of record see the Wednesday + Thursday refreshes captured
above this pass — both still apply. If `proposals/` remains empty
through 2026-W20 open, that is itself a finding (see Platform risks
→ "Empty proposal pipeline").

## Platform risks (carried forward, still active)

These are the cross-cutting observations from the W19 drafts that
remain unresolved going into 2026-W20. They are not re-litigated;
they are restated so the W20 cycle inherits them cleanly.

1. **`mcp__mctl__*` still missing — five passes blocked.** No live
   reconciliation against production image tags, open incidents, or
   tenant resource limits has been possible for five consecutive
   mentor cycles. Every Top-5 promotion this week was decided against
   proposal text alone. The W20 cycle cannot meaningfully audit the
   post-rollout state of any W19 Top-5 item without restoration.
   Treat as a blocking dependency on the W20 mentor cycle.

2. **`labs` tenant RAM gate — three measurement opportunities now in
   flight.** openclaw 2026.5.4 soak, ArgoCD v3.4 GA promotion, and
   the Loki v3.7.1 chart bump each produce a usable RSS data point on
   `labs`. The systemic issue is not "we lack a measurement
   opportunity" — it is "we keep meaning to record the measurements."
   Operational fix: require the operator to write the RSS reading
   into `_mentor/digest/` as part of every PR's merge checklist, the
   same shape as the test-required gate. Three corroborating data
   points unblock `nodejs-heap-cap-labs`.

3. **MCP-hardening release-train v1 — six proposals across two repos,
   one ADR needed.** The hardening triple (`mcp-rce-server-hardening`,
   `mcp-command-injection-hardening`, `mcp-input-validation`), the
   `mcp-csrf-origin-{validation,check}` pair, and the `mcp-go-v052-
   upgrade` library bump compose into one defence layer across
   `mctl-api` and `mctl-agent`. Without a unifying ADR — shared
   schema-registry shape, shared `MCP_ALLOWED_*` env-var family,
   shared audit-table convention, shared ratelimit thresholds — the
   two endpoints will drift, producing same-threat / asymmetric-defence
   / false-coverage. Anoint a forced ADR owner before any of the six
   PRs merges.

4. **Two parallel ArgoCD upgrade paths active simultaneously.**
   v3.3.8 informer-cache patch (superseded if GA ships), v3.4.0 GA
   (anchor of the cluster-security release window), v3.4.1 follow-up
   (cluster-generator label-format migration). Pick one branch and
   close the other: GA → v3.4.1 in the same release window
   (preferred, single soak), or GA only with v3.4.1 deferred to W20.
   The v3.3.8 patch is the holding pattern only if GA promotion slips
   past W19 close. Pair v3.4.1 with `mctl-agent/argocd-cluster-label-
   drift` — same label-format dependency.

5. **Cross-tenant privilege boundaries — six pass-overdue, seven
   layers added.** Loki SA-token read, over-scoped ArgoCD client
   token, Helm binary across three privileged pods, mcp-write authz,
   ESO secret-scope, Vault PKI, openclaw multi-tenant deployment —
   seven layers added in five weeks with zero systemic re-validations.
   No longer "overdue" — structurally absent. The W20 cycle either
   names a forced owner or formally de-prioritises the question (so
   the platform stops carrying it forward without action).

6. **Dup-cluster tax keeps growing (W18 ~30, Wed-W19 ~40,
   Thu-W19 ~50 slugs collapsed per pass).** The `supersedes:` field
   in the proposal template — recommended in W18, escalated in
   Wednesday's W19, blocking now — remains unimplemented. Linear
   trend continues into W20. Prioritise the template change above any
   individual security bump in W20, otherwise W20 spends > 50% of
   its cycle on de-duplication.

7. **CVE clustering still demands a release-train.** Cluster-security
   release window: ten components (ArgoCD v3.4.0 GA + Argo Workflows
   v3.7.14 + ESO chart 2.4.1 + Vault four-CVE bundle + Prometheus
   v3.11.3 + Loki v3.7.1 + Helm v4.1.4 + cert-manager v1.20.2 +
   Grafana patched-set + post-promotion ArgoCD v3.4.1). Data-plane
   release: openclaw 2026.5.4 + portal Backstage v1.50.4 twelve-bundle
   + Go 1.26.2 cross-repo + mcp-go v0.52.0 + MCP-hardening triple +
   ArgoCD client-token rotation + mctl-web wrangler 4.88.0 trio. Two
   ApplicationSet syncs per week is the budget the platform has
   implicitly chosen by the volume of CVE-class proposals; splitting
   further multiplies soak load for no audit benefit.

8. **Renovate platform-wide rollout decision still per-repo (three
   passes carried).** The `mctl-web/automated-dep-updates` pilot
   answers the one-repo question; the platform-wide question — every
   service vs centralised at `mctl-gitops` — sits unanswered. The
   `cloudflare-vite-plugin` CVE that Renovate would have caught is
   already in production (W18 Top-5 #2); the same class of miss is
   mathematically certain to recur on a 24 h researcher cadence.
   Approve the per-repo pilot for general rollout in W20 or force
   the centralised decision.

9. **`mctl-docs` is structurally behind the platform it documents.**
   `mctl-docs` is at `0.1.0` (created 2026-03-28) while `mctl-api`
   sits at `4.14.0` and `mctl-web` at `4.6.2`. The Thursday-refresh
   Block G batched ~20 doc proposals to ship in two waves; if more
   than three of Batch 1 slip into W20, the batching pattern is too
   coarse and should split per sibling service.

## Conflicts (this pass)

None — there are no new proposals to conflict with one another. The
conflict matrix from the Wednesday + Thursday W19 drafts remains in
force; see those drafts for the carry-forward list (Loki rollout vs
`labs` RAM gate, ArgoCD client-token rotation timing, MCP layer
ordering, mctl-agent MCP pair single-PR rule, Anthropic SDK timeout
vs MCP TimeoutHandler, Helm binary fan-out vs scaffolder, workerd
version pair, openclaw outbound docs pair, openclaw discord/slack
libs bundle, baileys layered controls).

## Deferred (this pass)

Nothing was deferred this pass, because nothing new was submitted.
For clarity:

- No proposal was demoted from the Top-5 to W20.
- No proposal was rejected on grounds of conflict, fit, or effort.
- The deferral list from the Thursday-W19 refresh remains in force —
  see that draft for the full carry-forward list (~24 individual
  proposals + ~50 dup-cluster slugs).

## Checklist for next digest (W20)

1. **Restore `mcp__mctl__*`.** Five passes blocked. The W20 cycle
   cannot meaningfully audit any of the W19 Top-5 / Top-5-add-on
   rollouts without it. Items to reconcile when restored: openclaw
   2026.5.4 (or 2026.5.6 if that becomes the target), Backstage
   v1.50.4 twelve-bundle, ArgoCD v3.4.0 GA + v3.4.1, mcp-go v0.52.0,
   Go 1.26.2 toolchain on `mctl-api` and `mctl-agent`, ESO chart
   2.4.1, Vault v2.0.0, Loki v3.7.1, Helm v4.1.4, cert-manager
   v1.20.2, Prometheus v3.11.3, Wrangler 4.88.0 + workerd
   v1.20260506.1 (V8 14.8), Node.js 22.22.2, MCP-hardening-triple
   deployed state, ArgoCD client-token scoped state.
2. **Confirm three `labs` RSS readings landed** (openclaw 5.4 soak,
   ArgoCD v3.4 GA promotion, Loki v3.7.1 bump). Three corroborating
   data points unblock `nodejs-heap-cap-labs`.
3. **Confirm the MCP-hardening ADR landed** before any of the six
   related PRs merge.
4. **Confirm the ArgoCD client-token rotation** ran in the
   post-`labs`-soak / pre-`admins`-promotion window.
5. **Confirm the cluster-security and data-plane release windows
   each shipped as one ApplicationSet sync** — two syncs total.
6. **Promote `supersedes:` field in the proposal template from
   recommended to required.** Six passes carried; the dup-cluster
   collapse rate is climbing linearly.
7. **Add a `track:` field** (`workaround` / `upstream-fix`) to the
   proposal template — the gateway-UI #73836 pair is the new
   pattern that motivates it.
8. **Force an owner for the cross-tenant isolation re-validation,**
   or formally de-prioritise the question.
9. **Force a decision on Renovate platform-wide rollout.**
10. **Confirm `openclaw-upgrade-cve-batch` 5.6 changelog vs 5.4** —
    if 5.6 carries fresh CVEs (CVE-2026-25253 ClawBleed CSWSH,
    CVE-2026-32922 priv-esc), it becomes the W20 upgrade target.
11. **Confirm the Loki SA-token rotation** ran post-bump (Top-5 #1
    pre-flight) — treat any window of exposed-and-reachable Loki as
    a possible exposure event, mirroring the W18
    `cloudflare-vite-plugin` rotation precedent.
12. **Track the `mctl-docs` Batch 1 ship rate.** If more than three
    of the thirteen doc PRs slip into W20, the batching pattern is
    too coarse and should split per sibling service.
13. **Investigate the empty `proposals/` and `inbox/` state** — if
    every service still shows zero new submissions through W20 open,
    confirm the per-service daily loop is firing and that
    `GITHUB_TOKEN` / MCP wiring is intact.

---

_Generated by the mentor agent. Sources:
`/app/agents/<service>/proposals/` (empty for all seven services),
`/app/agents/<service>/context/current-version.md`,
`/app/agents/<service>/CLAUDE.md`, prior W19 drafts at this path.
Live cluster state via `mcp__mctl__*`: unavailable in this session._
