# mctl Platform Weekly Digest
**Week 38 (Sep 15-21, 2026)**

## Executive Summary

**Platform Health:** Excellent. One warning incident (acknowledged), all core services healthy and synced.

**Proposal Activity:** 45+ active proposals across 10+ services. Fresh proposals cluster around agent-platform architecture, lifecycle ownership, and platform observability.

**Key Observation:** Four high-impact proposals from recent investigations unlock critical agent-platform features: human-input clarification, proposal-less PR adoption, quota handling, and the v2 agent registry. One API proposal (issue-283) blocked on merge conflict — needs triage.

---

## Top 5 Proposals Ready for Review

### 1. **Durable Agent Clarification Loop — HumanInputRequest/Response Contract**
- **Service:** mctl-agents
- **Slug:** issue-333-feat-human-input-durable-agent-clarifica
- **Status:** Proposed (fresh, ready for acceptance)
- **Impact:** 5/5 (foundational for agent-human interaction; unblocks issue-investigator clarification)
- **Effort:** 4/5 (Temporal wait state, request/response schema, ContextSnapshot integration)
- **Platform Fit:** Critical infrastructure for agent autonomy; required by mctl-api#261 and mctl-telegram#571
- **Conflicts:** None; parallel work can proceed on API/Telegram surfaces
- **Summary:** Add durable `HumanInputRequest`/`HumanInputResponse` primitives and a `WAITING_FOR_INPUT` workflow state. Allows agents to ask bounded clarification questions without blocking compute; models get evidence, humans provide answers via surfaces (Telegram, portal). Defines ADR-011 contract that mctl-api and mctl-telegram consume. Schema-only scope; surfaces owned by downstream repos.

---

### 2. **Adopt Proposal-Less Same-Repo PRs with Blocking Reviews into Shepherd Loop**
- **Service:** mctl-agents
- **Slug:** issue-334-feat-lifecycle-adopt-proposal-less-same
- **Status:** Proposed (fresh, ready for acceptance)
- **Impact:** 4/5 (unblocks direct PR remediation; resolves mctl-gitops#1073 pattern)
- **Effort:** 4/5 (PR discovery, state machine, adoption record, fix loop)
- **Platform Fit:** Closes gap in lifecycle ownership; enables shepherd to drive remediations on any PR
- **Conflicts:** Works with issue-292 (steward-owned repos), depends on ADR-010
- **Summary:** Shepherd discovers PRs opened directly (not via proposal) that carry blocking P1/P2 review findings, adopts them as `PRRef` records, and drives fix-attempt loop. No proposal directory created. Records repo, PR number, head SHA, reviewer, attempts to lightweight gitops state. Feature-flagged behind `lifecycle.rollout.mode()`. Aligns lifecycle ownership for all work (proposal-backed or direct).

---

### 3. **Distinguish Quota-Exhausted Implementer Run from No-Commits**
- **Service:** mctl-agents
- **Slug:** issue-364-run-implementer-records-a-quota-exhauste
- **Status:** Proposed (fresh, actionable)
- **Impact:** 3/5 (clarifies triage; enables auto-retry on quota reset)
- **Effort:** 2/5 (condition detection, exit code, `.status.yaml` block)
- **Platform Fit:** Improves error classification and observability
- **Conflicts:** None; mirrors investigator's existing rate-limit handling
- **Summary:** Detect Claude API 429 (quota exhausted) distinctly from agent failure. Write `rate_limited` block to proposal `.status.yaml` with account, reset time, rate limit type. Set status back to `accepted` so run auto-retries post-reset instead of parking in `needs-triage`. Shepherd treats rate-limited review attempts as transient (don't count against review-attempts budget). Operator can see "exhausted until 2026-09-22T14:00Z" instead of guessing.

---

### 4. **Migrate Cloudflare Zone Roots (mctl.ai, mctl.me, mctl.ru) to R2 Remote State**
- **Service:** mctl-gitops
- **Slug:** issue-1178-chore-cloudflare-iac-migrate-the-three-z
- **Status:** Proposed (fresh, operationally important)
- **Impact:** 3/5 (enables drift detection on zone roots; improves incident recovery)
- **Effort:** 3/5 (infrastructure change, migration in three sequential PRs)
- **Platform Fit:** Zone roots are only Cloudflare control plane with no drift detector
- **Conflicts:** None; gated by token setup (already exists)
- **Summary:** Move three zone root Terraform states from local to R2 bucket (`mctl-cloudflare-state`), enabling nightly drift detection and apply from CI. Today: zone changes applied by hand, then pinned in import blocks. After: apply through `cloudflare-apply.yml` under review gate. Three sequential PRs (mctl-ru → mctl-me → mctl-ai), each import-only apply (zero mutations). Self-test fixtures updated for new root list. State backup includes all five roots on next snapshot.

---

### 5. **Publish and Resolve v1alpha2 AgentDefinition + ExecutionProfile Bindings**
- **Service:** mctl-api
- **Slug:** issue-283-feat-agent-platform-publish-and-resolve
- **Status:** Needs-triage (merge conflict, 5 review attempts; ready to fix)
- **Impact:** 4/5 (unblocks agent registry v2; enables declarative resolver mode)
- **Effort:** 4/5 (new registry tables, publish/bind/resolve/rollback operations)
- **Platform Fit:** Critical blocker for mctl-agents resolver.py (`bindingSource: registry`); gated by ADR-007
- **Conflicts:** Upstream blocks: mctl-agents#227 (done), mctl-gitops#950 (done). Downstream unlocks: issue-investigator agent seeding, declarative profiles.
- **Summary:** Extend Postgres registry to version `AgentDefinition` specs (with source manifest provenance), `ExecutionProfile` specs (with policy ceilings), and atomic `ReleaseBinding` pairs per environment. Support publish, bind, resolve (by env or explicit pin), rollback. Keep v1 image/version paths byte-compatible for agents not migrated. Reject incompatible or disabled pairs at bind time. Seed issue-investigator v1alpha2 into shadow environment. Currently blocked by merge conflict with base; needs triage pass to resolve and re-attempt.

---

## Themed Proposal Groups

### Agent-Platform Architecture & Governance (5 proposals)
Core infrastructure for agent autonomy, lifecycle ownership, and execution identity.

- **issue-333** (human clarification) — Recommended, ACCEPT & QUEUE
- **issue-334** (proposal-less PR adoption) — Recommended, ACCEPT & QUEUE
- **issue-364** (quota exhaustion) — Recommended, ACCEPT & QUEUE
- issue-196 (execution identity) — Already accepted/implemented (W34)
- issue-352 (executor class) — Implemented (W38, recent)
- issue-226, -227 (agent-platform architecture) — Merged (W34+)

**Platform Risk:** Three large proposals land in same release cycle. Coordinate rollout: ADR-011 (issue-333) → surfaces consume it → run investigator with clarification enabled.

**Sequencing:** issue-333 schema (ADR-011) first → issue-334 state machine follows → issue-364 is independent.

---

### Platform Observability & Reliability (3 proposals)
Drift detection, state management, quota handling.

- **issue-1178** (Cloudflare zones to R2) — Recommended
- issue-283 (v1alpha2 registry) — Blocked on merge conflict; triage & retry
- issue-1159 (Prometheus pushgateway duplicate scrapes) — Lower priority

**Status:** Zone migration unblocks drift for production DNS control plane. Registry unblocks agent resolver declarative mode.

---

### Lifecycle & Ownership Policy (2+ proposals)
ADR-010 follow-through; proposal-less PR discovery; steward ownership.

- **issue-334** (PR adoption) — Top-5 candidate
- issue-292 (steward-owned repos) — Merged
- issue-239 (dev-loop shepherd ownership) — Merged

**Note:** ADR-010 tables and primitives shipped; issue-334 is the first enforcement use case.

---

### Infrastructure & CI (6+ proposals)
Security, toolchain upgrades, GitOps reliability.

**High priority:**
- issue-1033 (MCTL_AGENTS GitHub App private key rotation) — Merged
- issue-1037 (OpenClaw version pin) — Merged

**Lower priority (infrastructure hygiene):**
- issue-1034 (GitHub App token v3 deprecation) — Proposed
- issue-1040 (release drift pagination) — Proposed
- issue-1041 (drop product-token requirement) — Proposed
- issue-1085 (custom domain DNS verification) — Proposed

---

### UI/UX, Docs, and Content (10+ proposals)
Spread across mctl-academy, mctl-pairdesk, mctl-design, mctl-docs.

**Status:** Low platform impact; defer to next cycle unless user-reported. Examples:
- mctl-academy: issue-14 (smoke test), issue-18-22 (content & UI)
- mctl-pairdesk: issue-22-45 (order flow redesign)
- mctl-design/mctl-docs: READMEs, storybook alignment

---

## Platform Risks & Cross-Cutting Observations

### 1. **Issue-283 (v1alpha2 Registry) Stuck on Merge Conflict (5 attempts)**
**Risk:** Blocker for resolver declarative mode; no progress for 4 days.
**Mitigation:**
- Triage the merge conflict: rebase or cherry-pick required changes from base
- Re-run implementer with --review-feedback to fix conflict + test
- If implementer cannot resolve, hand to implementer with explicit conflict guidance

---

### 2. **Three Architecture Proposals in Same Wave (issue-333, -334, -364)**
**Risk:** Large changes interact; testing matrix explodes if sequenced poorly.
**Mitigation:**
- Land issue-364 (quota handling) first; independent, low-effort, improves error surface
- ADR-011 schema (issue-333) can ship in parallel; surfaces depend on it but don't block it
- issue-334 (PR adoption) brings lifecycle machinery into shepherd; sequence after -333 surfaces are wired
- Explicitly test each pair in integration (issue-333 + shepherd, issue-334 + proposal-backed flow)

---

### 3. **Cloudflare Zone Migration (issue-1178) Unblocks Production Observability**
**Status:** Three zone roots (mctl.ai, mctl.me, mctl.ru) are the only production DNS with no drift detector.
**Impact:** Any out-of-band change (CNAME, ruleset, zone setting) goes undetected.
**Timeline:** Sequential PRs over 1-2 weeks. Low risk (import-only, zero mutations to Cloudflare).

---

### 4. **No Active Implementing or Review-Fixing Proposals**
**Observation:** All top-5 are in `proposed` state; none are actively in-flight.
**Implication:**
- Healthy signal: no stalled work or review loops
- Risk: Acceptance → implementation lag could re-surface if implementer is overloaded
- Action: Move top-5 to `accepted` this week; implement starting next cycle or parallel if capacity allows

---

### 5. **Platform Health Remains Excellent**
- One warning incident (acknowledged, workflow_failed on mctl-agents-implement, resolved Sep 12)
- All core services synced and healthy (last update < 10 min ago)
- Incident responder succeeding hourly; no blocking conditions

---

## Deferred Proposals (Not in Top 5)

### Medium-Effort Infrastructure (Defer; non-blocking)
- issue-1034 (GitHub App token v3 deprecation) — Housekeeping, not urgent
- issue-1040 (release drift pagination) — Proposed, lower priority
- issue-1041 (drop product-token requirement) — Proposed, hygiene
- issue-1085 (custom domain DNS verification) — Proposed, feature-complete path exists

**Rationale:** All defer gracefully. No SLA pressure; keep on radar for next cycle if bandwidth available.

---

### UI/UX Improvements (Defer; polish)
- mctl-pairdesk/issue-22-45 (order flow redesign) — Large scope, no blocker for core platform
- mctl-pairdesk/issue-52, -53 (UX polish) — Nice-to-have, defer unless user escalates
- mctl-academy, mctl-design proposals (docs, storybook) — Routine; batch in next cycle

**Rationale:** These are polish and content work; platform core functions without them.

---

### Content & Operational (Defer; routine)
- mctl-academy/issue-81 (GitHub sign-in migration) — Needs stakeholder alignment first
- mctl-academy/issue-83 (audit PR 64 evidence) — Routine content review
- issue-239 (dev-loop shepherd ownership) — Merged (done)
- issue-352 (executor class) — Implemented (done)

---

## Recommendation Summary

| # | Proposal | Service | Action | Timing | Notes |
|---|---|---|---|---|---|
| 1 | issue-333 | mctl-agents | Accept & queue for implementer | This week | ADR-011 schema; surfaces follow |
| 2 | issue-334 | mctl-agents | Accept & queue for implementer | This week | Lifecycle machinery in shepherd |
| 3 | issue-364 | mctl-agents | Accept & queue for implementer | This week or parallel | Low-effort, independent |
| 4 | issue-1178 | mctl-gitops | Accept & queue for implementer | This week; 3 sequential PRs | Unblocks zone drift detection |
| 5 | issue-283 | mctl-api | Triage merge conflict, retry implementer | This week | Blocker for resolver v2 |
| — | issue-1034, -1040, -1041, -1085 | mctl-gitops | Defer or batch | Next cycle | Infrastructure hygiene |
| — | mctl-pairdesk, -academy, -design issues | multi | Defer | Next cycle | UX polish, docs, content |

---

## Process Notes

- W34 digest covered proposals through early September; this digest captures work from mid-September onwards
- Issue-283 visible in recent operations but stuck in `needs-triage` (merge conflict) since Sep 14 — recommend priority triage pass
- Four proposals accepted/implemented in the last 48 hours (issue-438, -265, -196, -294, -68, -352); platform momentum strong
- Incident responder running successfully every 30 minutes; no backlog of findings

---

**Digest compiled by mentor | Week 38 | All output in English | Platform health: excellent**
