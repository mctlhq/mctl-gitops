# Tasks: argocd-serversidediff-secret-exposure

- [ ] 1. Locate the Argo CD control-plane version pin (image tag or Helm
      chart version) in `platform-gitops/apps/` / `platform-gitops/bootstrap/`
      and determine the currently deployed Argo CD version. — DoD: exact
      version string identified and documented (e.g. in the PR description
      or a scratch note), with the file/line where it is pinned.
- [ ] 2. Compare the confirmed version against the affected ranges
      (3.2.0-3.2.11, 3.3.0-3.3.9) for CVE-2026-43824 / CVE-2026-42880
      (depends on 1). — DoD: written determination of "affected" or
      "not affected" with the CVE range cited.
- [ ] 3. IF affected: bump the version pin to the smallest safe patched
      release (>=3.2.12, >=3.3.10) or align to the current tracked minor
      (3.5.2) if the fleet already tracks that line (depends on 2). — DoD:
      git diff to the version pin file, Argo CD control plane redeployed via
      normal ArgoCD self-sync, `argocd version` (or equivalent) on the
      running instance confirms the patched build.
- [ ] 4. IF not affected: record the confirmation explicitly in
      `context/current-version.md` or a short note under `context/decisions/`
      so this CVE does not need re-triage later (depends on 2). — DoD: file
      committed stating the confirmed safe version and the CVE IDs checked.
- [ ] 5. Verify all existing ArgoCD Applications generated by `apps`,
      `tenants`, `openclaw-skills` ApplicationSets remain Synced/Healthy
      after any version change (depends on 3). — DoD: `argocd app list` (or
      mctl equivalent) shows no new OutOfSync/Degraded Applications
      attributable to the Argo CD upgrade.

## Tests
- [ ] T1. After patching, attempt a ServerSideDiff against a Secret-backed
      Application as a read-only user and confirm cleartext Secret data is
      no longer returned/rendered.
- [ ] T2. Confirm no ApplicationSet or Application manifest schema changes
      were introduced (diff limited to Argo CD control-plane version pin).
- [ ] T3. Confirm sync/health status of a representative sample of
      Applications across `admins`, `labs`, and other tenants post-upgrade.

## Rollback
Revert the version-pin commit (image tag / Helm chart version) in
`platform-gitops/apps/` or `platform-gitops/bootstrap/` and let ArgoCD
self-sync roll the control plane back to the previous build. Since no CRD
or ApplicationSet generator changes are made, rollback is a single git
revert with no data migration concerns.
