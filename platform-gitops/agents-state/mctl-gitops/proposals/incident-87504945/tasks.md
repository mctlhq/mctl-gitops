# Tasks: incident-87504945

1. [ ] Create the directory `platform-gitops/agents-state/` in mctl-gitops repository
2. [ ] Add a .gitkeep file to `platform-gitops/agents-state/.gitkeep` to ensure the directory is tracked by git
3. [ ] Verify the directory structure is correct by checking git status
4. [ ] Merge the change and monitor that mctl-agents reconciliation workflows no longer fail with state_dir not found errors
