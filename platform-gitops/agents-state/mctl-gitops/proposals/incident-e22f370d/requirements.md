# Superseded

This proposal was drafted here by mistake before the target service was
finalized. The actual fix requires a code change in the `mctl-telegram`
service, not a `mctl-gitops` config change, so the real proposal lives at:

`platform-gitops/agents-state/mctl-telegram/proposals/incident-e22f370d/`

No `.status.yaml` was ever written in this directory, so it was never picked
up by the Tier 2 implementer.
