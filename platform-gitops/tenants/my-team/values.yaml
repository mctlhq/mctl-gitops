# Tenant configuration for: my-team
# Created by: wft-create-tenant workflow
# Managed by: ArgoCD ApplicationSet (applicationset-tenants)
tenant:
  name: my-team
  displayName: "my-team"
  description: ""
  contactEmail: ""
  members:
    - userId: mctl-reviewer
      role: owner
  quotas:
    requests.cpu: "200m"
    requests.memory: "1Gi"
    limits.cpu: "1.5"
    limits.memory: "3Gi"
    pods: "5"
    persistentvolumeclaims: "1"
    services: "2"
  limitRange:
    enabled: true
    default:
      cpu: "500m"
      memory: "256Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    max:
      cpu: "1"
      memory: "2Gi"
  networking:
    allowIntraNamespace: true
    allowClusterEgress: true
    allowInternetEgress: true
