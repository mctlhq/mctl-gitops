# Tenant configuration for: developers
# Managed by: platform-gitops/tenants/developers/values.yaml
# Applied by: ArgoCD ApplicationSet (applicationset-tenants)
tenant:
  name: developers
  displayName: "Developers"
  githubTeam: developers
  quotas:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "8"
    limits.memory: "16Gi"
    pods: "20"
    persistentvolumeclaims: "5"
    services: "10"
  limitRange:
    enabled: true
    default:
      cpu: "500m"
      memory: "256Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    max:
      cpu: "2"
      memory: "4Gi"
  networking:
    allowIntraNamespace: true
    allowClusterEgress: true
    allowInternetEgress: false
