# Tenant configuration for: mcp-smoke-8sajlb
# Created by: wft-create-tenant workflow
# Managed by: ArgoCD ApplicationSet (applicationset-tenants)
tenant:
  name: mcp-smoke-8sajlb
  displayName: "MCP Smoke mcp-smoke-8sajlb"
  description: "Ephemeral MCP smoke tenant"
  contactEmail: "platform-smoke@mctl.ai"
  members:
    - userId: mashkovd
      role: owner
  quotas:
    requests.cpu: "500m"
    requests.memory: "1280Mi"
    limits.cpu: "2"
    limits.memory: "4Gi"
    pods: "10"
    persistentvolumeclaims: "5"
    services: "10"
  limitRange:
    enabled: true
    default:
      cpu: "500m"
      memory: "256Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
    max:
      cpu: "2"
      memory: "5Gi"
  networking:
    allowIntraNamespace: true
    allowClusterEgress: true
    allowInternetEgress: true
