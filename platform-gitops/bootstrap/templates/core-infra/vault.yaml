apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://helm.releases.hashicorp.com
    chart: vault
    targetRevision: 0.28.1
    helm:
      values: |
        server:
          ha:
            enabled: true
            replicas: 1
            raft:
              enabled: true
              config: |
                ui = true

                listener "tcp" {
                  tls_disable = 1
                  address = "[::]:8200"
                  cluster_address = "[::]:8201"
                }

                storage "raft" {
                  path = "/vault/data"
                }

                service_registration "kubernetes" {}

          standalone:
            enabled: false

          dataStorage:
            enabled: true
            size: 1Gi
            storageClass: hcloud-volumes

          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi

          ingress:
            enabled: true
            ingressClassName: traefik
            annotations:
              cert-manager.io/cluster-issuer: letsencrypt-prod
            hosts:
              - host: secrets.mctl.ai
              - host: secrets.mctl.ai
            tls:
              - hosts:
                  - secrets.mctl.ai
                  - secrets.mctl.ai
                secretName: vault-tls

        ui:
          enabled: true

        injector:
          enabled: false

  ignoreDifferences:
    # ESO admission webhook injects defaults absent in git, plus legacy
    # last-applied-configuration annotations can pin stale apiVersion across
    # chart churn. Mirror the tenants ApplicationSet pattern.
    - group: external-secrets.io
      kind: ExternalSecret
      jqPathExpressions:
        - .metadata.annotations
        - .metadata.managedFields
        - .spec.target.deletionPolicy
        - .spec.data[].remoteRef.conversionStrategy
        - .spec.data[].remoteRef.decodingStrategy
        - .spec.data[].remoteRef.metadataPolicy
        - .spec.dataFrom[].extract.conversionStrategy
        - .spec.dataFrom[].extract.decodingStrategy
        - .spec.dataFrom[].extract.metadataPolicy
        - .status
    # Hashicorp vault chart 0.28.1 renders the StatefulSet pod template
    # with `metadata.annotations: null` (helper template emits an
    # `annotations:` line with no body when neither `server.annotations`
    # nor `server.includeConfigAnnotation` is set) and `spec.hostNetwork:
    # false`; the API server normalises both away. The controller would
    # otherwise flag the StatefulSet OutOfSync indefinitely even though
    # the SSA-applied state matches git intent.
    # `spec.volumeClaimTemplates[].metadata.creationTimestamp` is the
    # standard kube emits-`null` quirk on PVC templates.
    - group: apps
      kind: StatefulSet
      name: vault
      jqPathExpressions:
        - .spec.template.metadata.annotations
        - .spec.template.spec.hostNetwork
        - .spec.volumeClaimTemplates[].metadata.creationTimestamp
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: vault
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
      - ServerSideDiff=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
