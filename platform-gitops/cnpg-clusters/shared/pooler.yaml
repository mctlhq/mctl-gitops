apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: shared-pg-pooler-rw
spec:
  cluster:
    name: shared-pg
  instances: 2  # 2 PgBouncer pods for availability
  type: rw
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "400"
      default_pool_size: "2"
      reserve_pool_size: "1"
      max_db_connections: "20"    # max backend connections PER database
      server_idle_timeout: "300"
