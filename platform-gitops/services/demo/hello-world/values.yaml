# Service: hello-world
# Team: demo

# Chart: base-service

image:
  repository: ghcr.io/mctlhq/hello-world
  tag: "main"
imagePullSecrets:
  - name: ghcr-credentials
service:
  port: 8080
resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
env:
  APP_ENV: production
ingress:
  enabled: true
  hosts:
    - demo-hello-world.mctl.ai
    - demo-hello-world.mctl.me
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  tls:
    - secretName: hello-world-tls
      hosts:
        - demo-hello-world.mctl.ai
        - demo-hello-world.mctl.me
