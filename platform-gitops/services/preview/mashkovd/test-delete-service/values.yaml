image:
  repository: nginx
  tag: "latest"

service:
  port: 80

ingress:
  enabled: true
  host: test-delete.preview.mctl.me

replicas: 1

env:
  APP_NAME: "test-delete-service"

secretRef: test-delete-service-secrets
