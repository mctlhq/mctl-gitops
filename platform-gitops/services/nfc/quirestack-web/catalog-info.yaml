apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: quirestack-web
  namespace: nfc
  description: "quirestack-web service"
  annotations:
    backstage.io/kubernetes-label-selector: app.kubernetes.io/instance=nfc-quirestack-web
    backstage.io/kubernetes-id: quirestack-web
    backstage.io/kubernetes-namespace: nfc
    argocd/app-name: nfc-quirestack-web
    github.com/source-repo: WannaBeGeekster/quirestack
    # mctl.me/auto-deploy — triggered by GitHub App webhook on tag push
    #   "true" or "auto" — deploy immediately via scaffolder
    #   "confirm"         — notify only, user deploys manually from UI
    #   "false" or absent — skip
    mctl.me/auto-deploy: "false"
  links:
    - url: https://ops.mctl.ai/applications/argocd/nfc-quirestack-web
      title: ArgoCD
      icon: dashboard
    - url: https://github.com/WannaBeGeekster/quirestack
      title: Source Repository
      icon: github
    - url: https://workflows.mctl.ai
      title: Argo Workflows
      icon: github
    - url: https://nfc-quirestack-web.mctl.ai
      title: Service URL
      icon: web
  labels:
    team: nfc
    env: apps
spec:
  type: service
  lifecycle: production
  owner: group:default/nfc
  system: system:default/team-nfc
