# Spring worker: smoke-svc
# Team: mcp-smoke-v3nwm5
# Chart: base-service (ingress disabled)

image:
  repository: ghcr.io/mctlhq/smoke-svc
  tag: "main"
imagePullSecrets:
  - name: ghcr-credentials
ingress:
  enabled: false
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi
probes:
  startup:
    path: /actuator/health
    port: http
    initialDelaySeconds: 5
    periodSeconds: 5
    failureThreshold: 36
  readiness:
    path: /actuator/health
    port: http
    initialDelaySeconds: 15
    periodSeconds: 5
  liveness:
    path: /actuator/health
    port: http
    initialDelaySeconds: 90
    periodSeconds: 15
env: {}
envFrom:
  - secretRef:
      name: mcp-smoke-v3nwm5-smoke-svc-db-creds
dbSecret:
  vaultPath: teams/mcp-smoke-v3nwm5/smoke-svc/database
  secretName: mcp-smoke-v3nwm5-smoke-svc-db-creds
