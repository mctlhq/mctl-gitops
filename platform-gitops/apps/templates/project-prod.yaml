{{- /*
  TODO: Enable when prod cluster is ready
  Copy project-preview.yaml, change:
  - name: prod
  - description: Production environment
  - Restrict namespaces
  - Add approval requirements
*/ -}}
