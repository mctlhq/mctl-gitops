# ReleaseBindingIntent fixture -- NOT a real registry release. See
# issue-investigator.yaml's header comment for why (no real mctl-api
# v1alpha2 rows exist yet). gitSha is a placeholder.
apiVersion: agents.mctl.ai/v1alpha2
kind: ReleaseBindingIntent
metadata:
  agent: implementer
  environment: shadow
spec:
  sourceManifest:
    repo: mctlhq/mctl-agents
    path: agents/_manifests/implementer/agent.yaml
    gitSha: "deadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
  bindingSource: compatibility-fixture
  promotable: false
  registryLifecycle:
    definition: published
    profile: published
  definition:
    name: implementer
    version: "3"
    profileCompatibility: ">=1.0.0 <2.0.0"
  profile:
    name: implementer-default
    version: "1.0.0"
  bindingRevision: 12
  previousBindingRevision: 11
  history:
    - bindingRevision: 11
      definition: {name: implementer, version: "2"}
      profile: {name: implementer-default, version: "1.0.0"}
    - bindingRevision: 10
      definition: {name: implementer, version: "2"}
      profile: {name: implementer-default, version: "1.0.0"}
