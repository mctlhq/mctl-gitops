# ReleaseBindingIntent fixture -- NOT a real registry release. See
# issue-investigator.yaml's header comment for why (no real mctl-api
# v1alpha2 rows exist yet). gitSha is a placeholder.
apiVersion: agents.mctl.ai/v1alpha2
kind: ReleaseBindingIntent
metadata:
  agent: shepherd
  environment: shadow
spec:
  sourceManifest:
    repo: mctlhq/mctl-agents
    path: agents/_manifests/shepherd/agent.yaml
    gitSha: "deadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
    # sha256 of the AgentDefinition at `path`. Required since the
    # definition-pin fix: an OPTIONAL pin is not a gate, and these two
    # bindings validated green with the definition floating while
    # issue-investigator was pinned (claude P2 on this PR).
    contentHash: "sha256:c985510309726ded7d515590d3f8e509b100c22a59de9e4b358aca2808d98901"
  bindingSource: compatibility-fixture
  promotable: false
  registryLifecycle:
    definition: published
    profile: published
  definition:
    name: shepherd
    version: "1"
    profileCompatibility: ">=1.0.0 <2.0.0"
  profile:
    name: shepherd-default
    version: "1.0.0"
  bindingRevision: 4
  previousBindingRevision: 3
  history:
    - bindingRevision: 2
      definition: {name: shepherd, version: "1"}
      profile: {name: shepherd-default, version: "1.0.0"}
